{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "stacks-action-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stacks-action-operator.labels" . | nindent 4 }}
automountServiceAccountToken: true
{{- end }}
