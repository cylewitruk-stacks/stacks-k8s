---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: stackssigners.stacks.stacks.org
spec:
  group: stacks.stacks.org
  names:
    kind: StacksSigner
    listKind: StacksSignerList
    plural: stackssigners
    singular: stackssigner
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: StacksSigner is an independently owned reusable declaration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec contains reusable inputs.
            properties:
              accountRef:
                description: AccountRef supplies consensus signing identity.
                properties:
                  name:
                    description: Name is the referenced name; the containing field
                      determines its kind.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              config:
                description: Config selects configuration customization.
                properties:
                  compatibility:
                    description: Compatibility permits intentionally divergent experimental
                      actors.
                    enum:
                    - Managed
                    - Unverified
                    type: string
                  overrides:
                    description: Overrides contains TOML-compatible values validated
                      by the resolver.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  secretRef:
                    description: SecretRef selects a complete configuration version.
                    properties:
                      key:
                        description: Key selects one entry.
                        maxLength: 253
                        minLength: 1
                        type: string
                      name:
                        description: Name selects a same-namespace Secret.
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  serviceRefs:
                    description: ServiceRefs resolves explicit placeholders.
                    items:
                      description: ServiceRef resolves a named placeholder to participant
                        Service DNS.
                      properties:
                        alias:
                          description: Alias identifies the placeholder.
                          maxLength: 63
                          type: string
                        endpoint:
                          description: Endpoint selects the advertised port role.
                          enum:
                          - rpc
                          - p2p
                          - events
                          type: string
                        kind:
                          description: Kind restricts the target API kind.
                          enum:
                          - BitcoinNode
                          - StacksNode
                          - StacksSigner
                          type: string
                        name:
                          description: Name identifies a selected participant.
                          maxLength: 63
                          type: string
                      required:
                      - alias
                      - endpoint
                      - kind
                      - name
                      type: object
                    maxItems: 100
                    type: array
                    x-kubernetes-list-map-keys:
                    - alias
                    x-kubernetes-list-type: map
                type: object
                x-kubernetes-validations:
                - message: config overrides and secretRef are exclusive
                  rule: '!(has(self.overrides) && has(self.secretRef))'
              image:
                description: Image selects the actor binary.
                maxLength: 512
                type: string
              imagePullPolicy:
                description: ImagePullPolicy controls image retrieval.
                enum:
                - Always
                - IfNotPresent
                - Never
                type: string
              nodeRef:
                description: NodeRef selects the associated StacksNode participant.
                properties:
                  name:
                    description: Name is the referenced name; the containing field
                      determines its kind.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              placement:
                description: Placement applies only to actor Pods.
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector constrains eligible nodes.
                    maxProperties: 32
                    type: object
                  spreadAcrossNodes:
                    description: SpreadAcrossNodes requests preferred same-kind anti-affinity.
                    type: boolean
                  tolerations:
                    description: Tolerations allow explicit support-node placement.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    maxItems: 32
                    type: array
                type: object
              resources:
                description: Resources are workload requests/limits.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storage:
                description: Storage configures instance-local persistence.
                properties:
                  class:
                    description: Class preserves explicit empty storageClassName.
                    maxLength: 253
                    type: string
                  ephemeral:
                    description: Ephemeral selects emptyDir.
                    type: boolean
                  retainOnDelete:
                    description: RetainOnDelete preserves a PVC after instance removal.
                    type: boolean
                  size:
                    description: Size is a Kubernetes capacity quantity.
                    maxLength: 32
                    type: string
                type: object
                x-kubernetes-validations:
                - message: ephemeral storage excludes PVC settings
                  rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                    && !has(self.class) && !has(self.retainOnDelete))'
            type: object
          status:
            description: Status reports direct-input resolution only.
            properties:
              bitcoinAddress:
                description: BitcoinAddress is the resolved public wallet payout address.
                type: string
              conditions:
                description: Conditions report resolution without runtime claims.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              credentialsRef:
                description: CredentialsRef identifies generated or imported private
                  material.
                properties:
                  key:
                    description: Key selects one entry.
                    maxLength: 253
                    minLength: 1
                    type: string
                  name:
                    description: Name selects a same-namespace Secret.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - key
                - name
                type: object
              credentialsUID:
                description: CredentialsUID pins imported or generated key material
                  after resolution.
                type: string
              dependencies:
                description: Dependencies pins public source identities.
                items:
                  description: Binding identifies an exact resolved public dependency.
                  properties:
                    fingerprint:
                      description: Fingerprint identifies public content when relevant.
                      type: string
                    kind:
                      description: Kind identifies the API kind or participant kind.
                      type: string
                    name:
                      description: Name is the public resource name.
                      type: string
                    uid:
                      description: UID prevents same-name replacement from inheriting
                        authority.
                      type: string
                  required:
                  - kind
                  - name
                  - uid
                  type: object
                maxItems: 8
                type: array
              descriptor:
                description: Descriptor is the supported public wallet descriptor.
                type: string
              digest:
                description: Digest identifies resolved public inputs.
                type: string
              identity:
                description: Identity contains public account information.
                properties:
                  address:
                    description: Address is a testnet Stacks address.
                    maxLength: 64
                    pattern: ^ST[0-9A-HJKMNP-TV-Z]{26,39}$
                    type: string
                  publicKey:
                    description: PublicKey is a compressed secp256k1 public key.
                    pattern: ^(02|03)[0-9a-fA-F]{64}$
                    type: string
                required:
                - address
                - publicKey
                type: object
              observedGeneration:
                description: ObservedGeneration identifies the evaluated spec.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
