---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: stacksnetworkparticipants.network.stacks.org
spec:
  group: network.stacks.org
  names:
    kind: StacksNetworkParticipant
    listKind: StacksNetworkParticipantList
    plural: stacksnetworkparticipants
    singular: stacksnetworkparticipant
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: StacksNetworkParticipant is generated, never a reusable declaration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec contains aggregate-owned compiled policy.
            properties:
              configuration:
                description: Configuration contains complete compiled values.
                properties:
                  bitcoinBlockProduction:
                    description: BitcoinBlockProduction configures baseline Bitcoin
                      opportunities.
                    properties:
                      initialization:
                        description: Initialization freezes initial funding behavior.
                        properties:
                          matureOutputsPerMiner:
                            description: MatureOutputsPerMiner is required for every
                              bootstrap miner.
                            format: int32
                            maximum: 100
                            minimum: 1
                            type: integer
                          minerWalletRefs:
                            description: MinerWalletRefs selects wallets receiving
                              mature coinbase outputs.
                            items:
                              description: NameRef selects a resource or participant
                                in the same namespace.
                              properties:
                                name:
                                  description: Name is the referenced name; the containing
                                    field determines its kind.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          minimumHeight:
                            description: MinimumHeight is the initial Bitcoin height.
                            format: int64
                            maximum: 4294967295
                            minimum: 1
                            type: integer
                          targetNodeRef:
                            description: TargetNodeRef selects the initial generation
                              target.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - matureOutputsPerMiner
                        - minimumHeight
                        - targetNodeRef
                        type: object
                      payoutWalletRef:
                        description: PayoutWalletRef supplies baseline payout identity.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      schedule:
                        description: Schedule supplies inline timing.
                        properties:
                          cadence:
                            description: Cadence controls block opportunities.
                            properties:
                              interval:
                                description: Interval is the fixed duration.
                                maxLength: 32
                                minLength: 2
                                type: string
                              maximumInterval:
                                description: MaximumInterval bounds uniform timing.
                                maxLength: 32
                                minLength: 2
                                type: string
                              minimumInterval:
                                description: MinimumInterval bounds uniform timing.
                                maxLength: 32
                                minLength: 2
                                type: string
                              mode:
                                description: Mode selects fixed or uniform timing.
                                enum:
                                - Fixed
                                - Uniform
                                type: string
                            required:
                            - mode
                            type: object
                            x-kubernetes-validations:
                            - message: interval must be between 1s and 1h
                              rule: '!has(self.interval) || (duration(self.interval)
                                >= duration(''1s'') && duration(self.interval) <=
                                duration(''1h''))'
                            - message: minimumInterval must be between 1s and 1h
                              rule: '!has(self.minimumInterval) || (duration(self.minimumInterval)
                                >= duration(''1s'') && duration(self.minimumInterval)
                                <= duration(''1h''))'
                            - message: maximumInterval must be between 1s and 1h
                              rule: '!has(self.maximumInterval) || (duration(self.maximumInterval)
                                >= duration(''1s'') && duration(self.maximumInterval)
                                <= duration(''1h''))'
                            - message: maximumInterval must not be below minimumInterval
                              rule: '!has(self.minimumInterval) || !has(self.maximumInterval)
                                || duration(self.maximumInterval) >= duration(self.minimumInterval)'
                            - message: cadence fields must match mode
                              rule: 'self.mode == ''Fixed'' ? (has(self.interval)
                                && !has(self.minimumInterval) && !has(self.maximumInterval))
                                : (!has(self.interval) && has(self.minimumInterval)
                                && has(self.maximumInterval))'
                        required:
                        - cadence
                        type: object
                      scheduleRef:
                        description: ScheduleRef selects immutable reusable timing.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      targets:
                        description: Targets selects weighted generation targets.
                        items:
                          description: ProductionTarget gives one participant a positive
                            selection weight.
                          properties:
                            nodeRef:
                              description: NodeRef selects a BitcoinNode participant.
                              properties:
                                name:
                                  description: Name is the referenced name; the containing
                                    field determines its kind.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            weight:
                              description: Weight controls relative selection probability.
                              format: int32
                              minimum: 1
                              type: integer
                          required:
                          - nodeRef
                          - weight
                          type: object
                        maxItems: 100
                        type: array
                    type: object
                    x-kubernetes-validations:
                    - message: select one schedule source
                      rule: '!(has(self.scheduleRef) && has(self.schedule))'
                  bitcoinNode:
                    description: BitcoinNode configures Core.
                    properties:
                      config:
                        description: Config selects configuration customization.
                        properties:
                          compatibility:
                            description: Compatibility permits intentionally divergent
                              experimental actors.
                            enum:
                            - Managed
                            - Unverified
                            type: string
                          overrides:
                            description: Overrides contains Stacks TOML values or
                              Bitcoin scalar/repeated-option/section values.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          secretRef:
                            description: SecretRef selects a complete configuration
                              version.
                            properties:
                              key:
                                description: Key selects one entry.
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name selects a same-namespace Secret.
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          serviceRefs:
                            description: ServiceRefs resolves explicit placeholders.
                            items:
                              description: ServiceRef resolves a named placeholder
                                to participant Service DNS.
                              properties:
                                alias:
                                  description: Alias identifies the placeholder.
                                  maxLength: 63
                                  type: string
                                endpoint:
                                  description: Endpoint selects the advertised port
                                    role.
                                  enum:
                                  - rpc
                                  - p2p
                                  - events
                                  type: string
                                kind:
                                  description: Kind restricts the target API kind.
                                  enum:
                                  - BitcoinNode
                                  - StacksNode
                                  - StacksSigner
                                  type: string
                                name:
                                  description: Name identifies a selected participant.
                                  maxLength: 63
                                  type: string
                              required:
                              - alias
                              - endpoint
                              - kind
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - alias
                            x-kubernetes-list-type: map
                        type: object
                        x-kubernetes-validations:
                        - message: config overrides and secretRef are exclusive
                          rule: '!(has(self.overrides) && has(self.secretRef))'
                      image:
                        description: Image selects the actor binary.
                        maxLength: 512
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy controls image retrieval.
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                      peers:
                        description: Peers supplies startup seed hints.
                        properties:
                          discovery:
                            description: Discovery resolves the selected network graph.
                            enum:
                            - Network
                            type: string
                          nodeRefs:
                            description: NodeRefs identifies explicit participant
                              seeds.
                            items:
                              description: NameRef selects a resource or participant
                                in the same namespace.
                              properties:
                                name:
                                  description: Name is the referenced name; the containing
                                    field determines its kind.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                        type: object
                        x-kubernetes-validations:
                        - message: select nodeRefs or discovery
                          rule: has(self.nodeRefs) != has(self.discovery)
                      placement:
                        description: Placement applies only to actor Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                      resources:
                        description: Resources are workload requests/limits.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storage:
                        description: Storage configures instance-local persistence.
                        properties:
                          class:
                            description: Class preserves explicit empty storageClassName.
                            maxLength: 253
                            type: string
                          ephemeral:
                            description: Ephemeral selects emptyDir.
                            type: boolean
                          retainOnDelete:
                            description: RetainOnDelete preserves a PVC after instance
                              removal.
                            type: boolean
                          size:
                            description: Size is a Kubernetes capacity quantity.
                            maxLength: 32
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: ephemeral storage excludes PVC settings
                          rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                            && !has(self.class) && !has(self.retainOnDelete))'
                      walletRefs:
                        description: WalletRefs selects reusable wallet identities
                          loaded by this node.
                        items:
                          description: NameRef selects a resource or participant in
                            the same namespace.
                          properties:
                            name:
                              description: Name is the referenced name; the containing
                                field determines its kind.
                              maxLength: 253
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                              type: string
                          required:
                          - name
                          type: object
                        maxItems: 100
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      workerPlacement:
                        description: WorkerPlacement applies to scoped support Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                    type: object
                  stacksContractSet:
                    description: StacksContractSet configures required contracts.
                    properties:
                      bundle:
                        description: Bundle selects the immutable source manifest.
                        enum:
                        - sbtc-regtest-v1
                        type: string
                      deployerAccountRef:
                        description: DeployerAccountRef supplies deployment identity.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      initialization:
                        description: Initialization specifies registry postconditions.
                        properties:
                          aggregateKeyAccountRef:
                            description: AggregateKeyAccountRef supplies the explicit
                              aggregate key.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          mode:
                            description: Mode is the supported initialization authority.
                            enum:
                            - ExplicitTestRegistry
                            type: string
                          signerAccountRefs:
                            description: SignerAccountRefs supplies public bridge
                              identities.
                            items:
                              description: NameRef selects a resource or participant
                                in the same namespace.
                              properties:
                                name:
                                  description: Name is the referenced name; the containing
                                    field determines its kind.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            maxItems: 100
                            minItems: 2
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          threshold:
                            description: Threshold bounds registry authorization.
                            format: int32
                            maximum: 100
                            minimum: 1
                            type: integer
                        required:
                        - aggregateKeyAccountRef
                        - mode
                        - signerAccountRefs
                        - threshold
                        type: object
                        x-kubernetes-validations:
                        - message: threshold must not exceed signer count
                          rule: self.threshold <= size(self.signerAccountRefs)
                        - message: threshold must be a strict signer majority
                          rule: self.threshold > size(self.signerAccountRefs) / 2
                      targetNodeRef:
                        description: TargetNodeRef selects mutation ingress.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      workerPlacement:
                        description: WorkerPlacement applies to scoped support Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                    type: object
                  stacksFaucet:
                    description: StacksFaucet configures explicit funding.
                    properties:
                      accountRef:
                        description: AccountRef selects funding identity; omission
                          generates one.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      genesisBalanceMicroSTX:
                        description: GenesisBalanceMicroSTX contributes one immutable
                          allocation.
                        maxLength: 20
                        pattern: ^(0|[1-9][0-9]{0,19})$
                        type: string
                        x-kubernetes-validations:
                        - message: amount must fit uint64
                          rule: uint(self) <= 18446744073709551615u
                      maxRequestMicroSTX:
                        description: MaxRequestMicroSTX bounds each transfer.
                        maxLength: 20
                        pattern: ^(0|[1-9][0-9]{0,19})$
                        type: string
                        x-kubernetes-validations:
                        - message: amount must fit uint64
                          rule: uint(self) <= 18446744073709551615u
                      targetNodeRef:
                        description: TargetNodeRef selects native ingress.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      workerPlacement:
                        description: WorkerPlacement applies to scoped support Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                    type: object
                  stacksNode:
                    description: StacksNode configures a Stacks peer.
                    properties:
                      bitcoinNodeRef:
                        description: BitcoinNodeRef is a mandatory participant binding
                          after composition.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      config:
                        description: Config selects configuration customization.
                        properties:
                          compatibility:
                            description: Compatibility permits intentionally divergent
                              experimental actors.
                            enum:
                            - Managed
                            - Unverified
                            type: string
                          overrides:
                            description: Overrides contains Stacks TOML values or
                              Bitcoin scalar/repeated-option/section values.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          secretRef:
                            description: SecretRef selects a complete configuration
                              version.
                            properties:
                              key:
                                description: Key selects one entry.
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name selects a same-namespace Secret.
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          serviceRefs:
                            description: ServiceRefs resolves explicit placeholders.
                            items:
                              description: ServiceRef resolves a named placeholder
                                to participant Service DNS.
                              properties:
                                alias:
                                  description: Alias identifies the placeholder.
                                  maxLength: 63
                                  type: string
                                endpoint:
                                  description: Endpoint selects the advertised port
                                    role.
                                  enum:
                                  - rpc
                                  - p2p
                                  - events
                                  type: string
                                kind:
                                  description: Kind restricts the target API kind.
                                  enum:
                                  - BitcoinNode
                                  - StacksNode
                                  - StacksSigner
                                  type: string
                                name:
                                  description: Name identifies a selected participant.
                                  maxLength: 63
                                  type: string
                              required:
                              - alias
                              - endpoint
                              - kind
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - alias
                            x-kubernetes-list-type: map
                        type: object
                        x-kubernetes-validations:
                        - message: config overrides and secretRef are exclusive
                          rule: '!(has(self.overrides) && has(self.secretRef))'
                      identityAccountRef:
                        description: IdentityAccountRef supplies P2P/miner identity.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      image:
                        description: Image selects the actor binary.
                        maxLength: 512
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy controls image retrieval.
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                      mining:
                        description: Mining selects the optional miner role.
                        properties:
                          bitcoinWalletRef:
                            description: BitcoinWalletRef supplies miner funding identity,
                              including while disabled.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          enabled:
                            description: Enabled controls mining; omission is false.
                            type: boolean
                          feeRateSatsPerVByte:
                            description: FeeRateSatsPerVByte controls Bitcoin commit
                              fees.
                            format: int32
                            minimum: 1
                            type: integer
                        type: object
                      peers:
                        description: Peers supplies startup hints.
                        properties:
                          discovery:
                            description: Discovery resolves the selected network graph.
                            enum:
                            - Network
                            type: string
                          nodeRefs:
                            description: NodeRefs identifies explicit participant
                              seeds.
                            items:
                              description: NameRef selects a resource or participant
                                in the same namespace.
                              properties:
                                name:
                                  description: Name is the referenced name; the containing
                                    field determines its kind.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                        type: object
                        x-kubernetes-validations:
                        - message: select nodeRefs or discovery
                          rule: has(self.nodeRefs) != has(self.discovery)
                      placement:
                        description: Placement applies only to actor Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                      resources:
                        description: Resources are workload requests/limits.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storage:
                        description: Storage configures instance-local persistence.
                        properties:
                          class:
                            description: Class preserves explicit empty storageClassName.
                            maxLength: 253
                            type: string
                          ephemeral:
                            description: Ephemeral selects emptyDir.
                            type: boolean
                          retainOnDelete:
                            description: RetainOnDelete preserves a PVC after instance
                              removal.
                            type: boolean
                          size:
                            description: Size is a Kubernetes capacity quantity.
                            maxLength: 32
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: ephemeral storage excludes PVC settings
                          rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                            && !has(self.class) && !has(self.retainOnDelete))'
                    type: object
                  stacksSigner:
                    description: StacksSigner configures consensus signing.
                    properties:
                      accountRef:
                        description: AccountRef supplies consensus signing identity.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      config:
                        description: Config selects configuration customization.
                        properties:
                          compatibility:
                            description: Compatibility permits intentionally divergent
                              experimental actors.
                            enum:
                            - Managed
                            - Unverified
                            type: string
                          overrides:
                            description: Overrides contains Stacks TOML values or
                              Bitcoin scalar/repeated-option/section values.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          secretRef:
                            description: SecretRef selects a complete configuration
                              version.
                            properties:
                              key:
                                description: Key selects one entry.
                                maxLength: 253
                                minLength: 1
                                type: string
                              name:
                                description: Name selects a same-namespace Secret.
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          serviceRefs:
                            description: ServiceRefs resolves explicit placeholders.
                            items:
                              description: ServiceRef resolves a named placeholder
                                to participant Service DNS.
                              properties:
                                alias:
                                  description: Alias identifies the placeholder.
                                  maxLength: 63
                                  type: string
                                endpoint:
                                  description: Endpoint selects the advertised port
                                    role.
                                  enum:
                                  - rpc
                                  - p2p
                                  - events
                                  type: string
                                kind:
                                  description: Kind restricts the target API kind.
                                  enum:
                                  - BitcoinNode
                                  - StacksNode
                                  - StacksSigner
                                  type: string
                                name:
                                  description: Name identifies a selected participant.
                                  maxLength: 63
                                  type: string
                              required:
                              - alias
                              - endpoint
                              - kind
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - alias
                            x-kubernetes-list-type: map
                        type: object
                        x-kubernetes-validations:
                        - message: config overrides and secretRef are exclusive
                          rule: '!(has(self.overrides) && has(self.secretRef))'
                      image:
                        description: Image selects the actor binary.
                        maxLength: 512
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy controls image retrieval.
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                      nodeRef:
                        description: NodeRef selects the associated StacksNode participant.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      placement:
                        description: Placement applies only to actor Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                      resources:
                        description: Resources are workload requests/limits.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storage:
                        description: Storage configures instance-local persistence.
                        properties:
                          class:
                            description: Class preserves explicit empty storageClassName.
                            maxLength: 253
                            type: string
                          ephemeral:
                            description: Ephemeral selects emptyDir.
                            type: boolean
                          retainOnDelete:
                            description: RetainOnDelete preserves a PVC after instance
                              removal.
                            type: boolean
                          size:
                            description: Size is a Kubernetes capacity quantity.
                            maxLength: 32
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: ephemeral storage excludes PVC settings
                          rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                            && !has(self.class) && !has(self.retainOnDelete))'
                    type: object
                  stacksStacker:
                    description: StacksStacker configures protocol maintenance.
                    properties:
                      administratorAccountRef:
                        description: AdministratorAccountRef controls the direct manager.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      amountMicroSTX:
                        description: AmountMicroSTX is desired stake.
                        maxLength: 20
                        pattern: ^(0|[1-9][0-9]{0,19})$
                        type: string
                        x-kubernetes-validations:
                        - message: amount must fit uint64
                          rule: uint(self) <= 18446744073709551615u
                      holderAccountRef:
                        description: HolderAccountRef funds stake.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      lockCycles:
                        description: LockCycles controls enrollment coverage.
                        format: int32
                        maximum: 12
                        minimum: 2
                        type: integer
                      renewWhenRemainingCycles:
                        description: RenewWhenRemainingCycles triggers maintenance.
                        format: int32
                        maximum: 11
                        minimum: 1
                        type: integer
                      signerRef:
                        description: SignerRef identifies a consensus signer participant.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      targetNodeRef:
                        description: TargetNodeRef selects verified mutation ingress.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      workerPlacement:
                        description: WorkerPlacement applies to scoped support Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: renewal threshold must be below lock cycles
                      rule: '!has(self.lockCycles) || !has(self.renewWhenRemainingCycles)
                        || self.renewWhenRemainingCycles < self.lockCycles'
                  stacksTransactionProduction:
                    description: StacksTransactionProduction configures baseline traffic.
                    properties:
                      accountRef:
                        description: AccountRef selects signing identity.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      amountMicroSTX:
                        description: AmountMicroSTX is transfer amount.
                        maxLength: 20
                        pattern: ^(0|[1-9][0-9]{0,19})$
                        type: string
                        x-kubernetes-validations:
                        - message: amount must fit uint64
                          rule: uint(self) <= 18446744073709551615u
                      feeMicroSTX:
                        description: FeeMicroSTX is transfer fee.
                        maxLength: 20
                        pattern: ^(0|[1-9][0-9]{0,19})$
                        type: string
                        x-kubernetes-validations:
                        - message: amount must fit uint64
                          rule: uint(self) <= 18446744073709551615u
                      interval:
                        description: Interval is the offered cadence.
                        maxLength: 32
                        minLength: 2
                        type: string
                      recipient:
                        description: Recipient receives baseline transfers.
                        properties:
                          accountRef:
                            description: AccountRef selects a resolved public identity.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          address:
                            description: Address is an explicit destination.
                            maxLength: 64
                            pattern: ^ST[0-9A-HJKMNP-TV-Z]{26,39}$
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: select an account or address
                          rule: has(self.accountRef) != has(self.address)
                      targetNodeRef:
                        description: TargetNodeRef selects native ingress.
                        properties:
                          name:
                            description: Name is the referenced name; the containing
                              field determines its kind.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        type: object
                      workerPlacement:
                        description: WorkerPlacement applies to scoped support Pods.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: NodeSelector constrains eligible nodes.
                            maxProperties: 32
                            type: object
                          spreadAcrossNodes:
                            description: SpreadAcrossNodes requests preferred same-kind
                              anti-affinity.
                            type: boolean
                          tolerations:
                            description: Tolerations allow explicit support-node placement.
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            maxItems: 32
                            type: array
                        type: object
                    type: object
                type: object
              control:
                description: Control projects the user's workload-specific operation.
                properties:
                  paused:
                    description: Paused cooperatively holds supported baseline workers.
                    type: boolean
                  suspended:
                    description: Suspended scales actors down with retained storage.
                    type: boolean
                type: object
              kind:
                description: Kind selects a domain controller.
                enum:
                - BitcoinNode
                - StacksNode
                - StacksSigner
                - StacksStacker
                - StacksFaucet
                - StacksContractSet
                - StacksTransactionProduction
                - BitcoinBlockProduction
                type: string
              networkUID:
                description: NetworkUID binds the canonical root.
                type: string
              participantName:
                description: ParticipantName identifies the root map-list entry.
                type: string
              source:
                description: Source records pinned input provenance.
                properties:
                  digest:
                    description: Digest identifies inline or reusable public spec
                      content.
                    type: string
                  generation:
                    description: Generation identifies the resolved input revision.
                    format: int64
                    type: integer
                  name:
                    description: Name identifies a reusable definition; empty for
                      inline.
                    type: string
                  uid:
                    description: UID pins a reusable definition; empty for inline.
                    type: string
                required:
                - digest
                - generation
                type: object
            required:
            - configuration
            - kind
            - networkUID
            - participantName
            - source
            type: object
          status:
            description: Status separates aggregate admission, domain runtime and
              worker execution.
            properties:
              admission:
                description: Admission contains the complete accepted public policy.
                properties:
                  configuration:
                    description: Configuration retains public requirements before
                      runtime exists.
                    properties:
                      bitcoinBlockProduction:
                        description: BitcoinBlockProduction configures baseline Bitcoin
                          opportunities.
                        properties:
                          initialization:
                            description: Initialization freezes initial funding behavior.
                            properties:
                              matureOutputsPerMiner:
                                description: MatureOutputsPerMiner is required for
                                  every bootstrap miner.
                                format: int32
                                maximum: 100
                                minimum: 1
                                type: integer
                              minerWalletRefs:
                                description: MinerWalletRefs selects wallets receiving
                                  mature coinbase outputs.
                                items:
                                  description: NameRef selects a resource or participant
                                    in the same namespace.
                                  properties:
                                    name:
                                      description: Name is the referenced name; the
                                        containing field determines its kind.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                      type: string
                                  required:
                                  - name
                                  type: object
                                maxItems: 100
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              minimumHeight:
                                description: MinimumHeight is the initial Bitcoin
                                  height.
                                format: int64
                                maximum: 4294967295
                                minimum: 1
                                type: integer
                              targetNodeRef:
                                description: TargetNodeRef selects the initial generation
                                  target.
                                properties:
                                  name:
                                    description: Name is the referenced name; the
                                      containing field determines its kind.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                    type: string
                                required:
                                - name
                                type: object
                            required:
                            - matureOutputsPerMiner
                            - minimumHeight
                            - targetNodeRef
                            type: object
                          payoutWalletRef:
                            description: PayoutWalletRef supplies baseline payout
                              identity.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          schedule:
                            description: Schedule supplies inline timing.
                            properties:
                              cadence:
                                description: Cadence controls block opportunities.
                                properties:
                                  interval:
                                    description: Interval is the fixed duration.
                                    maxLength: 32
                                    minLength: 2
                                    type: string
                                  maximumInterval:
                                    description: MaximumInterval bounds uniform timing.
                                    maxLength: 32
                                    minLength: 2
                                    type: string
                                  minimumInterval:
                                    description: MinimumInterval bounds uniform timing.
                                    maxLength: 32
                                    minLength: 2
                                    type: string
                                  mode:
                                    description: Mode selects fixed or uniform timing.
                                    enum:
                                    - Fixed
                                    - Uniform
                                    type: string
                                required:
                                - mode
                                type: object
                                x-kubernetes-validations:
                                - message: interval must be between 1s and 1h
                                  rule: '!has(self.interval) || (duration(self.interval)
                                    >= duration(''1s'') && duration(self.interval)
                                    <= duration(''1h''))'
                                - message: minimumInterval must be between 1s and
                                    1h
                                  rule: '!has(self.minimumInterval) || (duration(self.minimumInterval)
                                    >= duration(''1s'') && duration(self.minimumInterval)
                                    <= duration(''1h''))'
                                - message: maximumInterval must be between 1s and
                                    1h
                                  rule: '!has(self.maximumInterval) || (duration(self.maximumInterval)
                                    >= duration(''1s'') && duration(self.maximumInterval)
                                    <= duration(''1h''))'
                                - message: maximumInterval must not be below minimumInterval
                                  rule: '!has(self.minimumInterval) || !has(self.maximumInterval)
                                    || duration(self.maximumInterval) >= duration(self.minimumInterval)'
                                - message: cadence fields must match mode
                                  rule: 'self.mode == ''Fixed'' ? (has(self.interval)
                                    && !has(self.minimumInterval) && !has(self.maximumInterval))
                                    : (!has(self.interval) && has(self.minimumInterval)
                                    && has(self.maximumInterval))'
                            required:
                            - cadence
                            type: object
                          scheduleRef:
                            description: ScheduleRef selects immutable reusable timing.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          targets:
                            description: Targets selects weighted generation targets.
                            items:
                              description: ProductionTarget gives one participant
                                a positive selection weight.
                              properties:
                                nodeRef:
                                  description: NodeRef selects a BitcoinNode participant.
                                  properties:
                                    name:
                                      description: Name is the referenced name; the
                                        containing field determines its kind.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                      type: string
                                  required:
                                  - name
                                  type: object
                                weight:
                                  description: Weight controls relative selection
                                    probability.
                                  format: int32
                                  minimum: 1
                                  type: integer
                              required:
                              - nodeRef
                              - weight
                              type: object
                            maxItems: 100
                            type: array
                        type: object
                        x-kubernetes-validations:
                        - message: select one schedule source
                          rule: '!(has(self.scheduleRef) && has(self.schedule))'
                      bitcoinNode:
                        description: BitcoinNode configures Core.
                        properties:
                          config:
                            description: Config selects configuration customization.
                            properties:
                              compatibility:
                                description: Compatibility permits intentionally divergent
                                  experimental actors.
                                enum:
                                - Managed
                                - Unverified
                                type: string
                              overrides:
                                description: Overrides contains Stacks TOML values
                                  or Bitcoin scalar/repeated-option/section values.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              secretRef:
                                description: SecretRef selects a complete configuration
                                  version.
                                properties:
                                  key:
                                    description: Key selects one entry.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  name:
                                    description: Name selects a same-namespace Secret.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              serviceRefs:
                                description: ServiceRefs resolves explicit placeholders.
                                items:
                                  description: ServiceRef resolves a named placeholder
                                    to participant Service DNS.
                                  properties:
                                    alias:
                                      description: Alias identifies the placeholder.
                                      maxLength: 63
                                      type: string
                                    endpoint:
                                      description: Endpoint selects the advertised
                                        port role.
                                      enum:
                                      - rpc
                                      - p2p
                                      - events
                                      type: string
                                    kind:
                                      description: Kind restricts the target API kind.
                                      enum:
                                      - BitcoinNode
                                      - StacksNode
                                      - StacksSigner
                                      type: string
                                    name:
                                      description: Name identifies a selected participant.
                                      maxLength: 63
                                      type: string
                                  required:
                                  - alias
                                  - endpoint
                                  - kind
                                  - name
                                  type: object
                                maxItems: 100
                                type: array
                                x-kubernetes-list-map-keys:
                                - alias
                                x-kubernetes-list-type: map
                            type: object
                            x-kubernetes-validations:
                            - message: config overrides and secretRef are exclusive
                              rule: '!(has(self.overrides) && has(self.secretRef))'
                          image:
                            description: Image selects the actor binary.
                            maxLength: 512
                            type: string
                          imagePullPolicy:
                            description: ImagePullPolicy controls image retrieval.
                            enum:
                            - Always
                            - IfNotPresent
                            - Never
                            type: string
                          peers:
                            description: Peers supplies startup seed hints.
                            properties:
                              discovery:
                                description: Discovery resolves the selected network
                                  graph.
                                enum:
                                - Network
                                type: string
                              nodeRefs:
                                description: NodeRefs identifies explicit participant
                                  seeds.
                                items:
                                  description: NameRef selects a resource or participant
                                    in the same namespace.
                                  properties:
                                    name:
                                      description: Name is the referenced name; the
                                        containing field determines its kind.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                      type: string
                                  required:
                                  - name
                                  type: object
                                maxItems: 100
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                            type: object
                            x-kubernetes-validations:
                            - message: select nodeRefs or discovery
                              rule: has(self.nodeRefs) != has(self.discovery)
                          placement:
                            description: Placement applies only to actor Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                          resources:
                            description: Resources are workload requests/limits.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            description: Storage configures instance-local persistence.
                            properties:
                              class:
                                description: Class preserves explicit empty storageClassName.
                                maxLength: 253
                                type: string
                              ephemeral:
                                description: Ephemeral selects emptyDir.
                                type: boolean
                              retainOnDelete:
                                description: RetainOnDelete preserves a PVC after
                                  instance removal.
                                type: boolean
                              size:
                                description: Size is a Kubernetes capacity quantity.
                                maxLength: 32
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: ephemeral storage excludes PVC settings
                              rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                                && !has(self.class) && !has(self.retainOnDelete))'
                          walletRefs:
                            description: WalletRefs selects reusable wallet identities
                              loaded by this node.
                            items:
                              description: NameRef selects a resource or participant
                                in the same namespace.
                              properties:
                                name:
                                  description: Name is the referenced name; the containing
                                    field determines its kind.
                                  maxLength: 253
                                  minLength: 1
                                  pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                  type: string
                              required:
                              - name
                              type: object
                            maxItems: 100
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          workerPlacement:
                            description: WorkerPlacement applies to scoped support
                              Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                        type: object
                      stacksContractSet:
                        description: StacksContractSet configures required contracts.
                        properties:
                          bundle:
                            description: Bundle selects the immutable source manifest.
                            enum:
                            - sbtc-regtest-v1
                            type: string
                          deployerAccountRef:
                            description: DeployerAccountRef supplies deployment identity.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          initialization:
                            description: Initialization specifies registry postconditions.
                            properties:
                              aggregateKeyAccountRef:
                                description: AggregateKeyAccountRef supplies the explicit
                                  aggregate key.
                                properties:
                                  name:
                                    description: Name is the referenced name; the
                                      containing field determines its kind.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                    type: string
                                required:
                                - name
                                type: object
                              mode:
                                description: Mode is the supported initialization
                                  authority.
                                enum:
                                - ExplicitTestRegistry
                                type: string
                              signerAccountRefs:
                                description: SignerAccountRefs supplies public bridge
                                  identities.
                                items:
                                  description: NameRef selects a resource or participant
                                    in the same namespace.
                                  properties:
                                    name:
                                      description: Name is the referenced name; the
                                        containing field determines its kind.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                      type: string
                                  required:
                                  - name
                                  type: object
                                maxItems: 100
                                minItems: 2
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              threshold:
                                description: Threshold bounds registry authorization.
                                format: int32
                                maximum: 100
                                minimum: 1
                                type: integer
                            required:
                            - aggregateKeyAccountRef
                            - mode
                            - signerAccountRefs
                            - threshold
                            type: object
                            x-kubernetes-validations:
                            - message: threshold must not exceed signer count
                              rule: self.threshold <= size(self.signerAccountRefs)
                            - message: threshold must be a strict signer majority
                              rule: self.threshold > size(self.signerAccountRefs)
                                / 2
                          targetNodeRef:
                            description: TargetNodeRef selects mutation ingress.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          workerPlacement:
                            description: WorkerPlacement applies to scoped support
                              Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                        type: object
                      stacksFaucet:
                        description: StacksFaucet configures explicit funding.
                        properties:
                          accountRef:
                            description: AccountRef selects funding identity; omission
                              generates one.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          genesisBalanceMicroSTX:
                            description: GenesisBalanceMicroSTX contributes one immutable
                              allocation.
                            maxLength: 20
                            pattern: ^(0|[1-9][0-9]{0,19})$
                            type: string
                            x-kubernetes-validations:
                            - message: amount must fit uint64
                              rule: uint(self) <= 18446744073709551615u
                          maxRequestMicroSTX:
                            description: MaxRequestMicroSTX bounds each transfer.
                            maxLength: 20
                            pattern: ^(0|[1-9][0-9]{0,19})$
                            type: string
                            x-kubernetes-validations:
                            - message: amount must fit uint64
                              rule: uint(self) <= 18446744073709551615u
                          targetNodeRef:
                            description: TargetNodeRef selects native ingress.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          workerPlacement:
                            description: WorkerPlacement applies to scoped support
                              Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                        type: object
                      stacksNode:
                        description: StacksNode configures a Stacks peer.
                        properties:
                          bitcoinNodeRef:
                            description: BitcoinNodeRef is a mandatory participant
                              binding after composition.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          config:
                            description: Config selects configuration customization.
                            properties:
                              compatibility:
                                description: Compatibility permits intentionally divergent
                                  experimental actors.
                                enum:
                                - Managed
                                - Unverified
                                type: string
                              overrides:
                                description: Overrides contains Stacks TOML values
                                  or Bitcoin scalar/repeated-option/section values.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              secretRef:
                                description: SecretRef selects a complete configuration
                                  version.
                                properties:
                                  key:
                                    description: Key selects one entry.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  name:
                                    description: Name selects a same-namespace Secret.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              serviceRefs:
                                description: ServiceRefs resolves explicit placeholders.
                                items:
                                  description: ServiceRef resolves a named placeholder
                                    to participant Service DNS.
                                  properties:
                                    alias:
                                      description: Alias identifies the placeholder.
                                      maxLength: 63
                                      type: string
                                    endpoint:
                                      description: Endpoint selects the advertised
                                        port role.
                                      enum:
                                      - rpc
                                      - p2p
                                      - events
                                      type: string
                                    kind:
                                      description: Kind restricts the target API kind.
                                      enum:
                                      - BitcoinNode
                                      - StacksNode
                                      - StacksSigner
                                      type: string
                                    name:
                                      description: Name identifies a selected participant.
                                      maxLength: 63
                                      type: string
                                  required:
                                  - alias
                                  - endpoint
                                  - kind
                                  - name
                                  type: object
                                maxItems: 100
                                type: array
                                x-kubernetes-list-map-keys:
                                - alias
                                x-kubernetes-list-type: map
                            type: object
                            x-kubernetes-validations:
                            - message: config overrides and secretRef are exclusive
                              rule: '!(has(self.overrides) && has(self.secretRef))'
                          identityAccountRef:
                            description: IdentityAccountRef supplies P2P/miner identity.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          image:
                            description: Image selects the actor binary.
                            maxLength: 512
                            type: string
                          imagePullPolicy:
                            description: ImagePullPolicy controls image retrieval.
                            enum:
                            - Always
                            - IfNotPresent
                            - Never
                            type: string
                          mining:
                            description: Mining selects the optional miner role.
                            properties:
                              bitcoinWalletRef:
                                description: BitcoinWalletRef supplies miner funding
                                  identity, including while disabled.
                                properties:
                                  name:
                                    description: Name is the referenced name; the
                                      containing field determines its kind.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                    type: string
                                required:
                                - name
                                type: object
                              enabled:
                                description: Enabled controls mining; omission is
                                  false.
                                type: boolean
                              feeRateSatsPerVByte:
                                description: FeeRateSatsPerVByte controls Bitcoin
                                  commit fees.
                                format: int32
                                minimum: 1
                                type: integer
                            type: object
                          peers:
                            description: Peers supplies startup hints.
                            properties:
                              discovery:
                                description: Discovery resolves the selected network
                                  graph.
                                enum:
                                - Network
                                type: string
                              nodeRefs:
                                description: NodeRefs identifies explicit participant
                                  seeds.
                                items:
                                  description: NameRef selects a resource or participant
                                    in the same namespace.
                                  properties:
                                    name:
                                      description: Name is the referenced name; the
                                        containing field determines its kind.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                      type: string
                                  required:
                                  - name
                                  type: object
                                maxItems: 100
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                            type: object
                            x-kubernetes-validations:
                            - message: select nodeRefs or discovery
                              rule: has(self.nodeRefs) != has(self.discovery)
                          placement:
                            description: Placement applies only to actor Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                          resources:
                            description: Resources are workload requests/limits.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            description: Storage configures instance-local persistence.
                            properties:
                              class:
                                description: Class preserves explicit empty storageClassName.
                                maxLength: 253
                                type: string
                              ephemeral:
                                description: Ephemeral selects emptyDir.
                                type: boolean
                              retainOnDelete:
                                description: RetainOnDelete preserves a PVC after
                                  instance removal.
                                type: boolean
                              size:
                                description: Size is a Kubernetes capacity quantity.
                                maxLength: 32
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: ephemeral storage excludes PVC settings
                              rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                                && !has(self.class) && !has(self.retainOnDelete))'
                        type: object
                      stacksSigner:
                        description: StacksSigner configures consensus signing.
                        properties:
                          accountRef:
                            description: AccountRef supplies consensus signing identity.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          config:
                            description: Config selects configuration customization.
                            properties:
                              compatibility:
                                description: Compatibility permits intentionally divergent
                                  experimental actors.
                                enum:
                                - Managed
                                - Unverified
                                type: string
                              overrides:
                                description: Overrides contains Stacks TOML values
                                  or Bitcoin scalar/repeated-option/section values.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              secretRef:
                                description: SecretRef selects a complete configuration
                                  version.
                                properties:
                                  key:
                                    description: Key selects one entry.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  name:
                                    description: Name selects a same-namespace Secret.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              serviceRefs:
                                description: ServiceRefs resolves explicit placeholders.
                                items:
                                  description: ServiceRef resolves a named placeholder
                                    to participant Service DNS.
                                  properties:
                                    alias:
                                      description: Alias identifies the placeholder.
                                      maxLength: 63
                                      type: string
                                    endpoint:
                                      description: Endpoint selects the advertised
                                        port role.
                                      enum:
                                      - rpc
                                      - p2p
                                      - events
                                      type: string
                                    kind:
                                      description: Kind restricts the target API kind.
                                      enum:
                                      - BitcoinNode
                                      - StacksNode
                                      - StacksSigner
                                      type: string
                                    name:
                                      description: Name identifies a selected participant.
                                      maxLength: 63
                                      type: string
                                  required:
                                  - alias
                                  - endpoint
                                  - kind
                                  - name
                                  type: object
                                maxItems: 100
                                type: array
                                x-kubernetes-list-map-keys:
                                - alias
                                x-kubernetes-list-type: map
                            type: object
                            x-kubernetes-validations:
                            - message: config overrides and secretRef are exclusive
                              rule: '!(has(self.overrides) && has(self.secretRef))'
                          image:
                            description: Image selects the actor binary.
                            maxLength: 512
                            type: string
                          imagePullPolicy:
                            description: ImagePullPolicy controls image retrieval.
                            enum:
                            - Always
                            - IfNotPresent
                            - Never
                            type: string
                          nodeRef:
                            description: NodeRef selects the associated StacksNode
                              participant.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          placement:
                            description: Placement applies only to actor Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                          resources:
                            description: Resources are workload requests/limits.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            description: Storage configures instance-local persistence.
                            properties:
                              class:
                                description: Class preserves explicit empty storageClassName.
                                maxLength: 253
                                type: string
                              ephemeral:
                                description: Ephemeral selects emptyDir.
                                type: boolean
                              retainOnDelete:
                                description: RetainOnDelete preserves a PVC after
                                  instance removal.
                                type: boolean
                              size:
                                description: Size is a Kubernetes capacity quantity.
                                maxLength: 32
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: ephemeral storage excludes PVC settings
                              rule: '!has(self.ephemeral) || !self.ephemeral || (!has(self.size)
                                && !has(self.class) && !has(self.retainOnDelete))'
                        type: object
                      stacksStacker:
                        description: StacksStacker configures protocol maintenance.
                        properties:
                          administratorAccountRef:
                            description: AdministratorAccountRef controls the direct
                              manager.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          amountMicroSTX:
                            description: AmountMicroSTX is desired stake.
                            maxLength: 20
                            pattern: ^(0|[1-9][0-9]{0,19})$
                            type: string
                            x-kubernetes-validations:
                            - message: amount must fit uint64
                              rule: uint(self) <= 18446744073709551615u
                          holderAccountRef:
                            description: HolderAccountRef funds stake.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          lockCycles:
                            description: LockCycles controls enrollment coverage.
                            format: int32
                            maximum: 12
                            minimum: 2
                            type: integer
                          renewWhenRemainingCycles:
                            description: RenewWhenRemainingCycles triggers maintenance.
                            format: int32
                            maximum: 11
                            minimum: 1
                            type: integer
                          signerRef:
                            description: SignerRef identifies a consensus signer participant.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          targetNodeRef:
                            description: TargetNodeRef selects verified mutation ingress.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          workerPlacement:
                            description: WorkerPlacement applies to scoped support
                              Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: renewal threshold must be below lock cycles
                          rule: '!has(self.lockCycles) || !has(self.renewWhenRemainingCycles)
                            || self.renewWhenRemainingCycles < self.lockCycles'
                      stacksTransactionProduction:
                        description: StacksTransactionProduction configures baseline
                          traffic.
                        properties:
                          accountRef:
                            description: AccountRef selects signing identity.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          amountMicroSTX:
                            description: AmountMicroSTX is transfer amount.
                            maxLength: 20
                            pattern: ^(0|[1-9][0-9]{0,19})$
                            type: string
                            x-kubernetes-validations:
                            - message: amount must fit uint64
                              rule: uint(self) <= 18446744073709551615u
                          feeMicroSTX:
                            description: FeeMicroSTX is transfer fee.
                            maxLength: 20
                            pattern: ^(0|[1-9][0-9]{0,19})$
                            type: string
                            x-kubernetes-validations:
                            - message: amount must fit uint64
                              rule: uint(self) <= 18446744073709551615u
                          interval:
                            description: Interval is the offered cadence.
                            maxLength: 32
                            minLength: 2
                            type: string
                          recipient:
                            description: Recipient receives baseline transfers.
                            properties:
                              accountRef:
                                description: AccountRef selects a resolved public
                                  identity.
                                properties:
                                  name:
                                    description: Name is the referenced name; the
                                      containing field determines its kind.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                    type: string
                                required:
                                - name
                                type: object
                              address:
                                description: Address is an explicit destination.
                                maxLength: 64
                                pattern: ^ST[0-9A-HJKMNP-TV-Z]{26,39}$
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: select an account or address
                              rule: has(self.accountRef) != has(self.address)
                          targetNodeRef:
                            description: TargetNodeRef selects native ingress.
                            properties:
                              name:
                                description: Name is the referenced name; the containing
                                  field determines its kind.
                                maxLength: 253
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                                type: string
                            required:
                            - name
                            type: object
                          workerPlacement:
                            description: WorkerPlacement applies to scoped support
                              Pods.
                            properties:
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector constrains eligible nodes.
                                maxProperties: 32
                                type: object
                              spreadAcrossNodes:
                                description: SpreadAcrossNodes requests preferred
                                  same-kind anti-affinity.
                                type: boolean
                              tolerations:
                                description: Tolerations allow explicit support-node
                                  placement.
                                items:
                                  description: |-
                                    The pod this Toleration is attached to tolerates any taint that matches
                                    the triple <key,value,effect> using the matching operator <operator>.
                                  properties:
                                    effect:
                                      description: |-
                                        Effect indicates the taint effect to match. Empty means match all taint effects.
                                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                      type: string
                                    key:
                                      description: |-
                                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                      type: string
                                    operator:
                                      description: |-
                                        Operator represents a key's relationship to the value.
                                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                        Exists is equivalent to wildcard for value, so that a pod can
                                        tolerate all taints of a particular category.
                                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                      type: string
                                    tolerationSeconds:
                                      description: |-
                                        TolerationSeconds represents the period of time the toleration (which must be
                                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                                        negative values will be treated as 0 (evict immediately) by the system.
                                      format: int64
                                      type: integer
                                    value:
                                      description: |-
                                        Value is the taint value the toleration matches to.
                                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                                      type: string
                                  type: object
                                maxItems: 32
                                type: array
                            type: object
                        type: object
                    type: object
                  dependencies:
                    description: Dependencies pins exact mandatory resource identities.
                    items:
                      description: Binding identifies an exact resolved public dependency.
                      properties:
                        fingerprint:
                          description: Fingerprint identifies public content when
                            relevant.
                          type: string
                        kind:
                          description: Kind identifies the API kind or participant
                            kind.
                          type: string
                        name:
                          description: Name is the public resource name.
                          type: string
                        uid:
                          description: UID prevents same-name replacement from inheriting
                            authority.
                          type: string
                      required:
                      - kind
                      - name
                      - uid
                      type: object
                    maxItems: 1000
                    type: array
                  policyDigest:
                    description: PolicyDigest identifies the whole admitted configuration.
                    type: string
                  source:
                    description: Source records the exact provenance of this retained
                      complete policy.
                    properties:
                      digest:
                        description: Digest identifies inline or reusable public spec
                          content.
                        type: string
                      generation:
                        description: Generation identifies the resolved input revision.
                        format: int64
                        type: integer
                      name:
                        description: Name identifies a reusable definition; empty
                          for inline.
                        type: string
                      uid:
                        description: UID pins a reusable definition; empty for inline.
                        type: string
                    required:
                    - digest
                    - generation
                    type: object
                required:
                - configuration
                - policyDigest
                - source
                type: object
              bitcoinControl:
                description: BitcoinControl contains domain-owned control process
                  termination evidence.
                properties:
                  deploymentRef:
                    description: DeploymentRef pins the actual control workload incarnation.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  networkGeneration:
                    description: NetworkGeneration identifies the evaluated root control
                      generation.
                    format: int64
                    type: integer
                  observedGeneration:
                    description: ObservedGeneration identifies the evaluated participant
                      generation.
                    format: int64
                    type: integer
                  pods:
                    description: Pods retains unresolved process identities and terminal
                      evidence until disposal.
                    items:
                      description: BitcoinControlPodStatus binds termination evidence
                        to one exact Pod and ReplicaSet.
                      properties:
                        deployment:
                          description: Deployment preserves the verified owner epoch
                            across control workload replacement.
                          properties:
                            fingerprint:
                              description: Fingerprint identifies public content when
                                relevant.
                              type: string
                            kind:
                              description: Kind identifies the API kind or participant
                                kind.
                              type: string
                            name:
                              description: Name is the public resource name.
                              type: string
                            uid:
                              description: UID prevents same-name replacement from
                                inheriting authority.
                              type: string
                          required:
                          - kind
                          - name
                          - uid
                          type: object
                        pod:
                          description: Pod names the exact observed process host.
                          properties:
                            fingerprint:
                              description: Fingerprint identifies public content when
                                relevant.
                              type: string
                            kind:
                              description: Kind identifies the API kind or participant
                                kind.
                              type: string
                            name:
                              description: Name is the public resource name.
                              type: string
                            uid:
                              description: UID prevents same-name replacement from
                                inheriting authority.
                              type: string
                          required:
                          - kind
                          - name
                          - uid
                          type: object
                        replicaSet:
                          description: ReplicaSet identifies the verified Deployment-owned
                            replica controller.
                          properties:
                            fingerprint:
                              description: Fingerprint identifies public content when
                                relevant.
                              type: string
                            kind:
                              description: Kind identifies the API kind or participant
                                kind.
                              type: string
                            name:
                              description: Name is the public resource name.
                              type: string
                            uid:
                              description: UID prevents same-name replacement from
                                inheriting authority.
                              type: string
                          required:
                          - kind
                          - name
                          - uid
                          type: object
                        terminated:
                          description: Terminated records confirmed exit, never inferred
                            from object disappearance.
                          type: boolean
                        uid:
                          description: UID is the immutable Pod identity and list
                            key.
                          type: string
                      required:
                      - deployment
                      - pod
                      - replicaSet
                      - terminated
                      - uid
                      type: object
                    maxItems: 128
                    type: array
                    x-kubernetes-list-map-keys:
                    - uid
                    x-kubernetes-list-type: map
                  reason:
                    description: Reason distinguishes observed shutdown from unavailable
                      termination evidence.
                    type: string
                  terminated:
                    description: Terminated requires confirmed exit of all processes
                      and no remaining replica intent.
                    type: boolean
                required:
                - networkGeneration
                - observedGeneration
                - reason
                - terminated
                type: object
              conditions:
                description: Conditions report resolution and replacement requirements.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              execution:
                description: Execution is owned only by the exact bound Stacks worker
                  process.
                properties:
                  administratorTransactions:
                    description: AdministratorTransactions contains the distinct administrator
                      account stream, when needed.
                    properties:
                      accepted:
                        description: Accepted counts exact transaction acknowledgements,
                          without claiming inclusion.
                        format: int64
                        type: integer
                      included:
                        description: Included counts exact transactions observed in
                          canonical blocks.
                        format: int64
                        type: integer
                      lastInclusion:
                        description: LastInclusion identifies the most recently observed
                          canonical inclusion.
                        properties:
                          blockID:
                            description: BlockID identifies the canonical block observed
                              by the target node.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                          observedAt:
                            description: ObservedAt preserves the original observation
                              time across status-write retries.
                            format: date-time
                            type: string
                          success:
                            description: Success reports the transaction execution
                              result.
                            type: boolean
                          txID:
                            description: TxID identifies the exact submitted transaction.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                        required:
                        - blockID
                        - observedAt
                        - success
                        - txID
                        type: object
                      lastPostcondition:
                        description: LastPostcondition retains the latest desired-state
                          observation, never transaction inclusion.
                        properties:
                          kind:
                            description: Kind names the bounded operation whose exact
                              public state was observed.
                            enum:
                            - PoX4Enrollment
                            - PoX4Extension
                            - ContractDeployment
                            - RegistryInitialization
                            - ManagerDeployment
                            - SignerRegistration
                            - PoX5Enrollment
                            - PoX5Extension
                            type: string
                          observedAt:
                            description: ObservedAt preserves the successful state
                              observation across API retries.
                            format: date-time
                            type: string
                          stacksTip:
                            description: StacksTip is the canonical index block ID
                              used for every pinned native read.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                          stateDigest:
                            description: StateDigest identifies the verified public
                              state, excluding observation time.
                            pattern: ^sha256:[0-9a-f]{64}$
                            type: string
                          txID:
                            description: TxID references the pending offered transaction;
                              it does not assert inclusion.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                        required:
                        - kind
                        - observedAt
                        - stacksTip
                        - stateDigest
                        - txID
                        type: object
                      lastTxID:
                        description: LastTxID identifies the most recent submission
                          attempt.
                        pattern: ^[0-9a-f]{64}$
                        type: string
                      offered:
                        description: Offered counts signed transactions whose one
                          submission attempt began.
                        format: int64
                        type: integer
                      postconditionObserved:
                        description: PostconditionObserved counts exact desired-state/next-nonce
                          observations without TxID attribution.
                        format: int64
                        minimum: 0
                        type: integer
                      rejected:
                        description: Rejected counts matching allowlisted native validation
                          refusals.
                        format: int64
                        type: integer
                      uncertain:
                        description: Uncertain counts attempts without an exact acknowledgement
                          or definite validation refusal.
                        format: int64
                        type: integer
                    required:
                    - accepted
                    - included
                    - offered
                    - rejected
                    - uncertain
                    type: object
                  appliedPolicyDigest:
                    description: AppliedPolicyDigest identifies the complete policy
                      adopted at a safe role boundary.
                    maxLength: 128
                    type: string
                  contracts:
                    description: Contracts retains exact pinned source and native
                      registry observations.
                    properties:
                      aggregatePublicKey:
                        description: AggregatePublicKey is the explicit registry aggregate
                          key.
                        pattern: ^(02|03)[0-9a-f]{64}$
                        type: string
                      bundle:
                        description: Bundle identifies the release-pinned source manifest.
                        enum:
                        - sbtc-regtest-v1
                        type: string
                      burnHeight:
                        description: BurnHeight is the target's processed canonical
                          Bitcoin height.
                        format: int64
                        minimum: 0
                        type: integer
                      complete:
                        description: Complete requires all source hashes and every
                          registry field to match.
                        type: boolean
                      deployer:
                        description: Deployer identifies the immutable deployment
                          principal.
                        maxLength: 64
                        type: string
                      observedAt:
                        description: ObservedAt preserves the successful observation
                          time across status publication retries.
                        format: date-time
                        type: string
                      signerPrincipal:
                        description: SignerPrincipal is the principal derived by the
                          pinned bootstrap contract from the same key tuple.
                        maxLength: 64
                        type: string
                      signerPublicKeys:
                        description: SignerPublicKeys preserves the exact declared
                          registry key order.
                        items:
                          maxLength: 66
                          pattern: ^(02|03)[0-9a-f]{64}$
                          type: string
                        maxItems: 100
                        minItems: 2
                        type: array
                        x-kubernetes-list-type: atomic
                      sourceDigest:
                        description: SourceDigest identifies the complete observed
                          name-to-source-hash map.
                        pattern: ^sha256:[0-9a-f]{64}$
                        type: string
                      stacksTip:
                        description: StacksTip is the canonical index block ID pinning
                          source, registry and account reads.
                        pattern: ^[0-9a-f]{64}$
                        type: string
                      threshold:
                        description: Threshold records the exact observed registry
                          quorum.
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                    required:
                    - aggregatePublicKey
                    - bundle
                    - burnHeight
                    - complete
                    - deployer
                    - observedAt
                    - signerPrincipal
                    - signerPublicKeys
                    - sourceDigest
                    - stacksTip
                    - threshold
                    type: object
                  faucet:
                    description: Faucet retains bounded request accounting for this
                      process.
                    properties:
                      active:
                        description: Active counts locally unresolved requests and
                          unpersisted outcomes.
                        format: int32
                        maximum: 1000
                        minimum: 0
                        type: integer
                      completed:
                        description: Completed counts exact successful transfer inclusions
                          observed by this process.
                        format: int64
                        minimum: 0
                        type: integer
                      expired:
                        description: Expired counts affirmative no-send deadline or
                          deletion acknowledgements.
                        format: int64
                        minimum: 0
                        type: integer
                      lastDeletedOutcome:
                        description: LastDeletedOutcome retains the latest deleted-request
                          evidence until process destruction.
                        properties:
                          execution:
                            description: Execution preserves bounded public facts;
                              signed bytes and nonces are excluded.
                            properties:
                              amountMicroSTX:
                                description: AmountMicroSTX is the exact micro-STX
                                  amount.
                                maxLength: 20
                                pattern: ^(0|[1-9][0-9]{0,19})$
                                type: string
                                x-kubernetes-validations:
                                - message: amount must fit uint64
                                  rule: uint(self) <= 18446744073709551615u
                              destination:
                                description: Destination and AmountMicroSTX retain
                                  the actual immutable transfer inputs.
                                maxLength: 64
                                type: string
                              faucetUID:
                                description: FaucetUID pins the admitted participant
                                  instance.
                                maxLength: 128
                                minLength: 1
                                type: string
                              inclusionBlockID:
                                description: InclusionBlockID identifies the native
                                  canonical inclusion, when observed.
                                pattern: ^[0-9a-f]{64}$
                                type: string
                              networkUID:
                                description: NetworkUID identifies the environment
                                  actually observed by this worker.
                                maxLength: 128
                                minLength: 1
                                type: string
                              noSend:
                                description: NoSend is affirmative worker evidence
                                  that dispatch never began.
                                type: boolean
                              observedAt:
                                description: ObservedAt is the original successful
                                  observation time retained through API retries.
                                format: date-time
                                type: string
                              phase:
                                description: Phase reports dispatch and exact native
                                  outcomes independently of projection.
                                enum:
                                - Submitted
                                - Completed
                                - Rejected
                                - Expired
                                - Inconclusive
                                type: string
                              processNonce:
                                description: ProcessNonce identifies the surviving
                                  process that owns pending coordination.
                                maxLength: 128
                                minLength: 1
                                type: string
                              reason:
                                description: Reason classifies the observed outcome
                                  without raw RPC bodies.
                                maxLength: 64
                                type: string
                              txID:
                                description: TxID is the exact signed transaction
                                  identity when dispatch began.
                                pattern: ^[0-9a-f]{64}$
                                type: string
                              workerUID:
                                description: WorkerUID pins the admitted Pod.
                                maxLength: 128
                                minLength: 1
                                type: string
                            required:
                            - amountMicroSTX
                            - destination
                            - faucetUID
                            - networkUID
                            - noSend
                            - observedAt
                            - phase
                            - processNonce
                            - reason
                            - workerUID
                            type: object
                            x-kubernetes-validations:
                            - message: no-send evidence cannot identify a submitted
                                transaction
                              rule: '!self.noSend || !has(self.txID) && (self.phase
                                == ''Rejected'' || self.phase == ''Expired'')'
                            - message: expiry requires affirmative no-send evidence
                              rule: self.phase != 'Expired' || self.noSend
                            - message: completion requires exact native inclusion
                              rule: self.phase != 'Completed' || !self.noSend && has(self.txID)
                                && has(self.inclusionBlockID)
                            - message: rejection requires no-send evidence, a classified
                                native refusal with txID, or exact failed inclusion
                              rule: self.phase != 'Rejected' || self.noSend || has(self.inclusionBlockID)
                                || has(self.txID) && self.reason in ['RejectedFeeTooLow',
                                'RejectedBadNonce', 'RejectedConflictingNonceInMempool',
                                'RejectedNotEnoughFunds']
                          request:
                            description: Request is the exact deleted request identity.
                            properties:
                              fingerprint:
                                description: Fingerprint pins resolved public content
                                  where applicable.
                                maxLength: 71
                                type: string
                              kind:
                                description: Kind identifies the bound public resource.
                                maxLength: 64
                                type: string
                              name:
                                description: Name identifies the same-namespace object.
                                maxLength: 253
                                minLength: 1
                                type: string
                              uid:
                                description: UID prevents identity replacement.
                                maxLength: 128
                                minLength: 1
                                type: string
                            required:
                            - kind
                            - name
                            - uid
                            type: object
                        required:
                        - execution
                        - request
                        type: object
                      orphaned:
                        description: Orphaned counts deleted requests whose transaction
                          outcome remains unresolved.
                        format: int32
                        maximum: 1000
                        minimum: 0
                        type: integer
                      rejected:
                        description: Rejected counts no-send refusals, classified
                          ingress refusals and failed inclusions.
                        format: int64
                        minimum: 0
                        type: integer
                    required:
                    - active
                    - completed
                    - expired
                    - orphaned
                    - rejected
                    type: object
                  networkGeneration:
                    description: NetworkGeneration identifies the root control acknowledged.
                    format: int64
                    type: integer
                  observedAt:
                    description: ObservedAt records the original successful state
                      observation.
                    format: date-time
                    type: string
                  observedGeneration:
                    description: ObservedGeneration identifies the participant controls
                      evaluated.
                    format: int64
                    type: integer
                  pending:
                    description: Pending counts unresolved local submissions without
                      exposing nonce history.
                    format: int32
                    maximum: 1000
                    minimum: 0
                    type: integer
                  phase:
                    description: Phase reports process-local execution without claiming
                      protocol completion.
                    enum:
                    - Inactive
                    - Active
                    - Paused
                    - Blocked
                    - Draining
                    - Settled
                    - Unsettled
                    - Unknown
                    - Failed
                    type: string
                  podUID:
                    description: PodUID identifies the worker receiving downward-API
                      identity.
                    type: string
                  pox4:
                    description: PoX4 retains the latest public native enrollment
                      observation.
                    properties:
                      amountMicroSTX:
                        description: AmountMicroSTX is the exact uint128 native stake,
                          encoded as canonical decimal.
                        maxLength: 39
                        pattern: ^(0|[1-9][0-9]*)$
                        type: string
                      burnHeight:
                        description: BurnHeight is the canonical processed Bitcoin
                          height during observation.
                        format: int64
                        minimum: 0
                        type: integer
                      endCycleExclusive:
                        description: EndCycleExclusive is the first uncovered reward
                          cycle.
                        format: int64
                        minimum: 1
                        type: integer
                      firstCycle:
                        description: FirstCycle starts the currently recorded reward
                          coverage.
                        format: int64
                        minimum: 0
                        type: integer
                      holder:
                        description: Holder identifies the directly locked account.
                        maxLength: 64
                        pattern: ^ST[0-9A-HJKMNP-TV-Z]{26,39}$
                        type: string
                      observedAt:
                        description: ObservedAt records the successful bracketed observation
                          time.
                        format: date-time
                        type: string
                      signerPublicKey:
                        description: SignerPublicKey is the compressed consensus key
                          in the target reward-set entry.
                        pattern: ^(02|03)[0-9a-f]{64}$
                        type: string
                      stacksTip:
                        description: StacksTip is the canonical index block ID bracketing
                          state and nonce reads.
                        pattern: ^[0-9a-f]{64}$
                        type: string
                      targetCycle:
                        description: TargetCycle is the frozen initial cycle or current
                          maintenance observation cycle.
                        format: int64
                        minimum: 0
                        type: integer
                      targetCycleMatched:
                        description: TargetCycleMatched requires exact holder, signer,
                          payout and stake in the reward-set entry.
                        type: boolean
                    required:
                    - amountMicroSTX
                    - burnHeight
                    - endCycleExclusive
                    - firstCycle
                    - holder
                    - observedAt
                    - signerPublicKey
                    - stacksTip
                    - targetCycle
                    - targetCycleMatched
                    type: object
                  pox5:
                    description: PoX5 retains exact native direct-manager enrollment
                      evidence.
                    properties:
                      amountMicroSTX:
                        description: AmountMicroSTX is the exact canonical uint128
                          stake.
                        maxLength: 39
                        pattern: ^[1-9][0-9]*$
                        type: string
                      burnHeight:
                        description: BurnHeight is the processed Bitcoin height in
                          the same canonical view.
                        format: int64
                        minimum: 0
                        type: integer
                      delegatedAmountMicroSTX:
                        description: DelegatedAmountMicroSTX is the manager's native
                          delegated amount for TargetCycle.
                        maxLength: 39
                        pattern: ^[1-9][0-9]*$
                        type: string
                      endCycleExclusive:
                        description: EndCycleExclusive is the first uncovered cycle.
                        format: int64
                        minimum: 1
                        type: integer
                      firstCycle:
                        description: FirstCycle starts the holder's current lock coverage.
                        format: int64
                        minimum: 0
                        type: integer
                      holder:
                        description: Holder is the non-custodial staker principal.
                        maxLength: 64
                        pattern: ^ST[0-9A-HJKMNP-TV-Z]{26,39}$
                        type: string
                      manager:
                        description: Manager is the administrator's exact direct-signer
                          contract principal.
                        maxLength: 80
                        pattern: ^ST[0-9A-HJKMNP-TV-Z]{26,39}\.direct-signer$
                        type: string
                      managerSourceDigest:
                        description: ManagerSourceDigest hashes the exact source including
                          its holder binding.
                        pattern: ^sha256:[0-9a-f]{64}$
                        type: string
                      observedAt:
                        description: ObservedAt records when the complete canonical
                          observation succeeded.
                        format: date-time
                        type: string
                      signerPublicKey:
                        description: SignerPublicKey is the registered, actively authorized
                          compressed consensus key.
                        pattern: ^(02|03)[0-9a-f]{64}$
                        type: string
                      stacksTip:
                        description: StacksTip is the canonical index block ID used
                          for every pinned read.
                        pattern: ^[0-9a-f]{64}$
                        type: string
                      targetCycle:
                        description: TargetCycle is the frozen bootstrap or current
                          maintenance cycle.
                        format: int64
                        minimum: 0
                        type: integer
                      targetCycleMatched:
                        description: TargetCycleMatched requires exact holder membership
                          and manager signer-set inclusion.
                        type: boolean
                      unlockHeight:
                        description: UnlockHeight is the native account lock boundary,
                          verified against reward-cycle conversion.
                        format: int64
                        minimum: 1
                        type: integer
                    required:
                    - amountMicroSTX
                    - burnHeight
                    - delegatedAmountMicroSTX
                    - endCycleExclusive
                    - firstCycle
                    - holder
                    - manager
                    - managerSourceDigest
                    - observedAt
                    - signerPublicKey
                    - stacksTip
                    - targetCycle
                    - targetCycleMatched
                    - unlockHeight
                    type: object
                  processNonce:
                    description: ProcessNonce prevents an accidental same-Pod process
                      restart from resuming execution.
                    maxLength: 64
                    type: string
                  profileDigest:
                    description: ProfileDigest identifies immutable execution inputs.
                    maxLength: 71
                    pattern: ^sha256:[0-9a-f]{64}$
                    type: string
                  reason:
                    description: Reason is a bounded public classification without
                      private error contents.
                    maxLength: 128
                    type: string
                  traffic:
                    description: Traffic retains current canonical availability independently
                      of original inclusion progress.
                    properties:
                      available:
                        description: Available distinguishes a completed canonical
                          read from unavailable native RPC.
                        type: boolean
                      blockID:
                        description: BlockID identifies its current canonical inclusion
                          block when found.
                        maxLength: 64
                        type: string
                      burnHeight:
                        description: BurnHeight is the native height at that observation.
                        format: int64
                        type: integer
                      effectiveIntervalSeconds:
                        description: EffectiveIntervalSeconds is the policy actually
                          adopted by the surviving role.
                        format: int64
                        maximum: 3600
                        minimum: 1
                        type: integer
                      found:
                        description: Found reports whether the exact transaction remains
                          canonically included.
                        type: boolean
                      genesisUID:
                        description: GenesisUID pins the frozen network chain identity.
                        maxLength: 128
                        type: string
                      indexBlockID:
                        description: IndexBlockID binds the coherent native canonical
                          observation.
                        maxLength: 64
                        type: string
                      observedAt:
                        description: ObservedAt retains the original successful recheck
                          time across RPC failures.
                        format: date-time
                        type: string
                      progressWindowSeconds:
                        description: ProgressWindowSeconds is max(progress grace,
                          three applied intervals) plus RPC allowance.
                        format: int64
                        maximum: 10810
                        type: integer
                      submittedBurnHeight:
                        description: SubmittedBurnHeight is the native height observed
                          before the original send.
                        format: int64
                        type: integer
                      success:
                        description: Success reports successful native execution when
                          Found is true.
                        type: boolean
                      targetConfigurationDigest:
                        description: TargetConfigurationDigest pins the original effective
                          actor configuration.
                        maxLength: 128
                        type: string
                      targetContainerID:
                        description: TargetContainerID pins the original actor process.
                        maxLength: 256
                        type: string
                      targetParticipantUID:
                        description: TargetParticipantUID pins the ingress participant
                          used for the original transfer.
                        maxLength: 128
                        type: string
                      targetPodUID:
                        description: TargetPodUID pins its original actor incarnation.
                        maxLength: 128
                        type: string
                      txID:
                        description: TxID identifies the last transaction whose inclusion
                          is being rechecked.
                        maxLength: 64
                        type: string
                    required:
                    - available
                    - burnHeight
                    - effectiveIntervalSeconds
                    - found
                    - genesisUID
                    - observedAt
                    - progressWindowSeconds
                    - submittedBurnHeight
                    - success
                    - targetConfigurationDigest
                    - targetContainerID
                    - targetParticipantUID
                    - targetPodUID
                    - txID
                    type: object
                  transactions:
                    description: Transactions contains cumulative submission facts
                      for this worker process.
                    properties:
                      accepted:
                        description: Accepted counts exact transaction acknowledgements,
                          without claiming inclusion.
                        format: int64
                        type: integer
                      included:
                        description: Included counts exact transactions observed in
                          canonical blocks.
                        format: int64
                        type: integer
                      lastInclusion:
                        description: LastInclusion identifies the most recently observed
                          canonical inclusion.
                        properties:
                          blockID:
                            description: BlockID identifies the canonical block observed
                              by the target node.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                          observedAt:
                            description: ObservedAt preserves the original observation
                              time across status-write retries.
                            format: date-time
                            type: string
                          success:
                            description: Success reports the transaction execution
                              result.
                            type: boolean
                          txID:
                            description: TxID identifies the exact submitted transaction.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                        required:
                        - blockID
                        - observedAt
                        - success
                        - txID
                        type: object
                      lastPostcondition:
                        description: LastPostcondition retains the latest desired-state
                          observation, never transaction inclusion.
                        properties:
                          kind:
                            description: Kind names the bounded operation whose exact
                              public state was observed.
                            enum:
                            - PoX4Enrollment
                            - PoX4Extension
                            - ContractDeployment
                            - RegistryInitialization
                            - ManagerDeployment
                            - SignerRegistration
                            - PoX5Enrollment
                            - PoX5Extension
                            type: string
                          observedAt:
                            description: ObservedAt preserves the successful state
                              observation across API retries.
                            format: date-time
                            type: string
                          stacksTip:
                            description: StacksTip is the canonical index block ID
                              used for every pinned native read.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                          stateDigest:
                            description: StateDigest identifies the verified public
                              state, excluding observation time.
                            pattern: ^sha256:[0-9a-f]{64}$
                            type: string
                          txID:
                            description: TxID references the pending offered transaction;
                              it does not assert inclusion.
                            pattern: ^[0-9a-f]{64}$
                            type: string
                        required:
                        - kind
                        - observedAt
                        - stacksTip
                        - stateDigest
                        - txID
                        type: object
                      lastTxID:
                        description: LastTxID identifies the most recent submission
                          attempt.
                        pattern: ^[0-9a-f]{64}$
                        type: string
                      offered:
                        description: Offered counts signed transactions whose one
                          submission attempt began.
                        format: int64
                        type: integer
                      postconditionObserved:
                        description: PostconditionObserved counts exact desired-state/next-nonce
                          observations without TxID attribution.
                        format: int64
                        minimum: 0
                        type: integer
                      rejected:
                        description: Rejected counts matching allowlisted native validation
                          refusals.
                        format: int64
                        type: integer
                      uncertain:
                        description: Uncertain counts attempts without an exact acknowledgement
                          or definite validation refusal.
                        format: int64
                        type: integer
                    required:
                    - accepted
                    - included
                    - offered
                    - rejected
                    - uncertain
                    type: object
                required:
                - networkGeneration
                - observedAt
                - observedGeneration
                - pending
                - phase
                - podUID
                - processNonce
                - profileDigest
                type: object
              runtime:
                description: Runtime contains domain-owned workload identity and readiness
                  observations.
                properties:
                  actorRPCSecretRef:
                    description: ActorRPCSecretRef identifies restricted protocol-client
                      credentials.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  configRef:
                    description: ConfigRef identifies immutable rendered server configuration.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  configurationDigest:
                    description: ConfigurationDigest identifies public rendering inputs,
                      including signer attachments.
                    type: string
                  containerID:
                    description: ContainerID identifies the current or last observed
                      actor process.
                    type: string
                  endpoints:
                    description: Endpoints contains actual Service addresses.
                    items:
                      description: RuntimeEndpoint exposes one identity-bound participant
                        Service.
                      properties:
                        host:
                          description: Host is the actual same-namespace Service DNS
                            name.
                          type: string
                        name:
                          description: Name identifies the protocol endpoint.
                          type: string
                        port:
                          description: Port is the protocol port.
                          format: int32
                          type: integer
                      required:
                      - host
                      - name
                      - port
                      type: object
                    maxItems: 8
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  eventAuthSecretRef:
                    description: EventAuthSecretRef binds the node-owned authentication
                      shared only with its signer.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  imageID:
                    description: ImageID records the kubelet-resolved actor image
                      digest.
                    type: string
                  observedGeneration:
                    description: ObservedGeneration identifies the participant controls/configuration
                      evaluated.
                    format: int64
                    type: integer
                  podIP:
                    description: PodIP is the native address observed with PodRef
                      and ContainerID.
                    maxLength: 45
                    type: string
                  podRef:
                    description: PodRef pins the most recently observed actor Pod.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  policyDigest:
                    description: PolicyDigest identifies the complete admitted policy
                      used by this runtime.
                    type: string
                  protocol:
                    description: Protocol contains read-only native Stacks progress
                      and prepared-set observations.
                    properties:
                      available:
                        description: Available requires a complete successful canonical
                          observation; failed reads withdraw it.
                        type: boolean
                      burnConsensusHash:
                        description: BurnConsensusHash identifies the native processed
                          Bitcoin fork.
                        maxLength: 40
                        type: string
                      burnHeight:
                        description: BurnHeight is the native node's processed Bitcoin
                          height.
                        format: int64
                        type: integer
                      configurationDigest:
                        description: ConfigurationDigest binds the complete public
                          actor configuration inputs.
                        maxLength: 71
                        type: string
                      containerID:
                        description: ContainerID pins the observed native node process.
                        maxLength: 256
                        type: string
                      cycleLength:
                        description: CycleLength is the native Bitcoin-block reward
                          period.
                        format: int64
                        type: integer
                      fullySynced:
                        description: FullySynced is the node's own synchronization
                          report.
                        type: boolean
                      genesisUID:
                        description: GenesisUID pins the immutable network bootstrap
                          requirements.
                        maxLength: 128
                        type: string
                      highestStacksHeight:
                        description: HighestStacksHeight retains the greatest successfully
                          observed height for this participant.
                        format: int64
                        type: integer
                      indexBlockID:
                        description: IndexBlockID pins all prepared-set reads to the
                          native canonical tip.
                        maxLength: 64
                        type: string
                      lastHeightAdvancedAt:
                        description: LastHeightAdvancedAt changes only when a strictly
                          greater Stacks height is observed.
                        format: date-time
                        type: string
                      networkID:
                        description: NetworkID preserves the full native uint32 chain
                          identifier.
                        format: int64
                        maximum: 4294967295
                        type: integer
                      observedAt:
                        description: ObservedAt is the successful observation time,
                          never refreshed by failed RPCs.
                        format: date-time
                        type: string
                      podUID:
                        description: PodUID pins the exact process workload.
                        maxLength: 128
                        type: string
                      poxBurnHeight:
                        description: PoXBurnHeight is the native PoX timing view's
                          processed height.
                        format: int64
                        type: integer
                      poxContract:
                        description: PoXContract is the active boot contract principal.
                        maxLength: 128
                        type: string
                      preparedSet:
                        description: PreparedSet reports the frozen active prepared-set
                          gate, when one is required.
                        properties:
                          available:
                            description: Available is false for Core's explicit not-available-yet
                              response.
                            type: boolean
                          cycle:
                            description: Cycle identifies the explicit frozen target
                              cycle.
                            format: int64
                            type: integer
                          observedAt:
                            description: ObservedAt is the original successful prepared-set
                              observation time.
                            format: date-time
                            type: string
                          signers:
                            description: Signers contains exact native membership
                              and weights.
                            items:
                              description: PreparedSignerObservation identifies one
                                native consensus signer entry.
                              properties:
                                publicKey:
                                  description: PublicKey identifies the compressed
                                    consensus key.
                                  maxLength: 66
                                  pattern: ^(02|03)[0-9a-f]{64}$
                                  type: string
                                stackedAmount:
                                  description: StackedAmount is the native uint128
                                    micro-STX amount.
                                  maxLength: 39
                                  type: string
                                weight:
                                  description: Weight is reported by Core and must
                                    not be derived from specification amounts.
                                  format: int64
                                  maximum: 4294967295
                                  minimum: 1
                                  type: integer
                              required:
                              - publicKey
                              - stackedAmount
                              - weight
                              type: object
                            maxItems: 1000
                            type: array
                            x-kubernetes-list-type: atomic
                          threshold:
                            description: Threshold is the protocol's uint128 micro-STX
                              threshold, encoded losslessly.
                            maxLength: 39
                            type: string
                          version:
                            description: Version is the native reward-set encoding.
                            format: int32
                            maximum: 1
                            type: integer
                        required:
                        - available
                        - cycle
                        - observedAt
                        - version
                        type: object
                      reason:
                        description: Reason is a bounded public observation classification.
                        maxLength: 64
                        type: string
                      rewardCycle:
                        description: RewardCycle is the current native reward cycle.
                        format: int64
                        type: integer
                      stacksHeight:
                        description: StacksHeight is the current canonical Stacks
                          height.
                        format: int64
                        type: integer
                      stacksTip:
                        description: StacksTip identifies the canonical Stacks header.
                        maxLength: 64
                        type: string
                    required:
                    - available
                    - burnConsensusHash
                    - burnHeight
                    - configurationDigest
                    - containerID
                    - cycleLength
                    - fullySynced
                    - genesisUID
                    - highestStacksHeight
                    - indexBlockID
                    - lastHeightAdvancedAt
                    - networkID
                    - observedAt
                    - podUID
                    - poxBurnHeight
                    - poxContract
                    - reason
                    - rewardCycle
                    - stacksHeight
                    - stacksTip
                    type: object
                  rpcSecretRef:
                    description: RPCSecretRef identifies this node's mutation-worker
                      credentials.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  terminated:
                    description: Terminated requires observed process termination
                      or proof no process was created.
                    type: boolean
                  workerCandidate:
                    description: WorkerCandidate reports an inactive standalone management
                      Pod for root binding.
                    properties:
                      pod:
                        description: Pod identifies the exact deterministic candidate
                          incarnation.
                        properties:
                          kind:
                            description: Kind is always the native Pod resource.
                            enum:
                            - Pod
                            maxLength: 3
                            type: string
                          name:
                            description: Name is the deterministic candidate resource
                              name.
                            maxLength: 253
                            type: string
                          uid:
                            description: UID identifies the exact Pod incarnation.
                            maxLength: 128
                            minLength: 1
                            type: string
                        required:
                        - kind
                        - name
                        - uid
                        type: object
                      profileDigest:
                        description: ProfileDigest identifies fixed image and mounted
                          key/config identities.
                        maxLength: 71
                        pattern: ^sha256:[0-9a-f]{64}$
                        type: string
                    required:
                    - pod
                    - profileDigest
                    type: object
                  workloadRefs:
                    description: WorkloadRefs identifies the actual owned workload
                      roots.
                    items:
                      description: Binding identifies an exact resolved public dependency.
                      properties:
                        fingerprint:
                          description: Fingerprint identifies public content when
                            relevant.
                          type: string
                        kind:
                          description: Kind identifies the API kind or participant
                            kind.
                          type: string
                        name:
                          description: Name is the public resource name.
                          type: string
                        uid:
                          description: UID prevents same-name replacement from inheriting
                            authority.
                          type: string
                      required:
                      - kind
                      - name
                      - uid
                      type: object
                    maxItems: 8
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                required:
                - observedGeneration
                - terminated
                type: object
              scheduling:
                description: Scheduling projects the production domain's effective
                  retained baseline state.
                properties:
                  acknowledged:
                    description: Acknowledged counts exact baseline generation receipts,
                      independently of assignment ordering.
                    format: int64
                    minimum: 0
                    type: integer
                  admissionDigest:
                    description: AdmissionDigest binds the complete admitted configuration
                      and dependency snapshots.
                    maxLength: 71
                    type: string
                  assigned:
                    description: Assigned counts selected offers published to their
                      sole execution record.
                    format: int64
                    minimum: 0
                    type: integer
                  eligibleTargets:
                    description: EligibleTargets counts currently ready admitted targets
                      independently of the committed draw.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  initialization:
                    description: Initialization identifies the retained network scheduler
                      authority.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  lastAcknowledgedAt:
                    description: LastAcknowledgedAt preserves the latest original
                      attributable baseline receipt time.
                    format: date-time
                    type: string
                  nextOpportunityAt:
                    description: NextOpportunityAt is the next durable opportunity
                      deadline, without catch-up work.
                    format: date-time
                    type: string
                  observedAt:
                    description: ObservedAt records when current baseline target availability
                      was checked.
                    format: date-time
                    type: string
                  opportunities:
                    description: Opportunities counts due baseline selections across
                      producer replacements.
                    format: int64
                    minimum: 0
                    type: integer
                  override:
                    description: Override identifies active temporary timing while
                      admission retains the latest baseline.
                    properties:
                      expiresAt:
                        description: ExpiresAt bounds future overridden opportunities.
                        format: date-time
                        type: string
                      override:
                        description: Override pins the user-owned request.
                        properties:
                          fingerprint:
                            description: Fingerprint identifies public content when
                              relevant.
                            type: string
                          kind:
                            description: Kind identifies the API kind or participant
                              kind.
                            type: string
                          name:
                            description: Name is the public resource name.
                            type: string
                          uid:
                            description: UID prevents same-name replacement from inheriting
                              authority.
                            type: string
                        required:
                        - kind
                        - name
                        - uid
                        type: object
                      production:
                        description: Production pins the admitted producer incarnation.
                        properties:
                          fingerprint:
                            description: Fingerprint identifies public content when
                              relevant.
                            type: string
                          kind:
                            description: Kind identifies the API kind or participant
                              kind.
                            type: string
                          name:
                            description: Name is the public resource name.
                            type: string
                          uid:
                            description: UID prevents same-name replacement from inheriting
                              authority.
                            type: string
                        required:
                        - kind
                        - name
                        - uid
                        type: object
                      schedule:
                        description: Schedule snapshots the effective cadence.
                        properties:
                          cadence:
                            description: Cadence controls block opportunities.
                            properties:
                              interval:
                                description: Interval is the fixed duration.
                                maxLength: 32
                                minLength: 2
                                type: string
                              maximumInterval:
                                description: MaximumInterval bounds uniform timing.
                                maxLength: 32
                                minLength: 2
                                type: string
                              minimumInterval:
                                description: MinimumInterval bounds uniform timing.
                                maxLength: 32
                                minLength: 2
                                type: string
                              mode:
                                description: Mode selects fixed or uniform timing.
                                enum:
                                - Fixed
                                - Uniform
                                type: string
                            required:
                            - mode
                            type: object
                            x-kubernetes-validations:
                            - message: interval must be between 1s and 1h
                              rule: '!has(self.interval) || (duration(self.interval)
                                >= duration(''1s'') && duration(self.interval) <=
                                duration(''1h''))'
                            - message: minimumInterval must be between 1s and 1h
                              rule: '!has(self.minimumInterval) || (duration(self.minimumInterval)
                                >= duration(''1s'') && duration(self.minimumInterval)
                                <= duration(''1h''))'
                            - message: maximumInterval must be between 1s and 1h
                              rule: '!has(self.maximumInterval) || (duration(self.maximumInterval)
                                >= duration(''1s'') && duration(self.maximumInterval)
                                <= duration(''1h''))'
                            - message: maximumInterval must not be below minimumInterval
                              rule: '!has(self.minimumInterval) || !has(self.maximumInterval)
                                || duration(self.maximumInterval) >= duration(self.minimumInterval)'
                            - message: cadence fields must match mode
                              rule: 'self.mode == ''Fixed'' ? (has(self.interval)
                                && !has(self.minimumInterval) && !has(self.maximumInterval))
                                : (!has(self.interval) && has(self.minimumInterval)
                                && has(self.maximumInterval))'
                        required:
                        - cadence
                        type: object
                      scheduleRef:
                        description: ScheduleRef pins a selected immutable reusable
                          schedule.
                        properties:
                          fingerprint:
                            description: Fingerprint identifies public content when
                              relevant.
                            type: string
                          kind:
                            description: Kind identifies the API kind or participant
                              kind.
                            type: string
                          name:
                            description: Name is the public resource name.
                            type: string
                          uid:
                            description: UID prevents same-name replacement from inheriting
                              authority.
                            type: string
                        required:
                        - kind
                        - name
                        - uid
                        type: object
                      startedAt:
                        description: StartedAt is the original successful activation
                          time.
                        format: date-time
                        type: string
                    required:
                    - expiresAt
                    - override
                    - production
                    - schedule
                    - startedAt
                    type: object
                  policyDigest:
                    description: PolicyDigest identifies the admitted production configuration.
                    maxLength: 71
                    type: string
                  progressWindowSeconds:
                    description: ProgressWindowSeconds bounds expected receipt progress
                      using the effective cadence upper bound.
                    format: int64
                    maximum: 10810
                    minimum: 130
                    type: integer
                  reason:
                    description: Reason classifies the current baseline scheduling
                      state.
                    maxLength: 128
                    type: string
                  schedule:
                    description: Schedule is the exact effective timing value.
                    properties:
                      cadence:
                        description: Cadence controls block opportunities.
                        properties:
                          interval:
                            description: Interval is the fixed duration.
                            maxLength: 32
                            minLength: 2
                            type: string
                          maximumInterval:
                            description: MaximumInterval bounds uniform timing.
                            maxLength: 32
                            minLength: 2
                            type: string
                          minimumInterval:
                            description: MinimumInterval bounds uniform timing.
                            maxLength: 32
                            minLength: 2
                            type: string
                          mode:
                            description: Mode selects fixed or uniform timing.
                            enum:
                            - Fixed
                            - Uniform
                            type: string
                        required:
                        - mode
                        type: object
                        x-kubernetes-validations:
                        - message: interval must be between 1s and 1h
                          rule: '!has(self.interval) || (duration(self.interval) >=
                            duration(''1s'') && duration(self.interval) <= duration(''1h''))'
                        - message: minimumInterval must be between 1s and 1h
                          rule: '!has(self.minimumInterval) || (duration(self.minimumInterval)
                            >= duration(''1s'') && duration(self.minimumInterval)
                            <= duration(''1h''))'
                        - message: maximumInterval must be between 1s and 1h
                          rule: '!has(self.maximumInterval) || (duration(self.maximumInterval)
                            >= duration(''1s'') && duration(self.maximumInterval)
                            <= duration(''1h''))'
                        - message: maximumInterval must not be below minimumInterval
                          rule: '!has(self.minimumInterval) || !has(self.maximumInterval)
                            || duration(self.maximumInterval) >= duration(self.minimumInterval)'
                        - message: cadence fields must match mode
                          rule: 'self.mode == ''Fixed'' ? (has(self.interval) && !has(self.minimumInterval)
                            && !has(self.maximumInterval)) : (!has(self.interval)
                            && has(self.minimumInterval) && has(self.maximumInterval))'
                    required:
                    - cadence
                    type: object
                  scheduleGeneration:
                    description: ScheduleGeneration records the observed immutable
                      schedule revision; zero means inline.
                    format: int64
                    minimum: 0
                    type: integer
                  scheduleRef:
                    description: ScheduleRef pins an admitted reusable schedule when
                      one was selected.
                    properties:
                      fingerprint:
                        description: Fingerprint identifies public content when relevant.
                        type: string
                      kind:
                        description: Kind identifies the API kind or participant kind.
                        type: string
                      name:
                        description: Name is the public resource name.
                        type: string
                      uid:
                        description: UID prevents same-name replacement from inheriting
                          authority.
                        type: string
                    required:
                    - kind
                    - name
                    - uid
                    type: object
                  skipped:
                    description: Skipped counts selections lost to unavailable or
                      busy targets without redraw.
                    format: int64
                    minimum: 0
                    type: integer
                  targets:
                    description: Targets preserves target order and exact participant
                      UIDs independently of health.
                    items:
                      description: BitcoinScheduledTarget preserves one admitted weighted
                        participant identity.
                      properties:
                        participant:
                          description: Participant binds the exact selected BitcoinNode
                            incarnation.
                          properties:
                            fingerprint:
                              description: Fingerprint identifies public content when
                                relevant.
                              type: string
                            kind:
                              description: Kind identifies the API kind or participant
                                kind.
                              type: string
                            name:
                              description: Name is the public resource name.
                              type: string
                            uid:
                              description: UID prevents same-name replacement from
                                inheriting authority.
                              type: string
                          required:
                          - kind
                          - name
                          - uid
                          type: object
                        weight:
                          description: Weight is sampled independently of target availability.
                          format: int32
                          minimum: 1
                          type: integer
                      required:
                      - participant
                      - weight
                      type: object
                    maxItems: 100
                    type: array
                    x-kubernetes-list-type: atomic
                  unassigned:
                    description: Unassigned counts selected opportunities that expired
                      or were withdrawn before assignment.
                    format: int64
                    minimum: 0
                    type: integer
                required:
                - acknowledged
                - admissionDigest
                - assigned
                - eligibleTargets
                - initialization
                - opportunities
                - policyDigest
                - reason
                - skipped
                - unassigned
                type: object
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: participant identity is immutable
          rule: self.spec.networkUID == oldSelf.spec.networkUID && self.spec.participantName
            == oldSelf.spec.participantName && self.spec.kind == oldSelf.spec.kind
    served: true
    storage: true
    subresources:
      status: {}
