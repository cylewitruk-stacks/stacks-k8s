---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: stacksnetworks.network.stacks.org
spec:
  group: network.stacks.org
  names:
    kind: StacksNetwork
    listKind: StacksNetworkList
    plural: stacksnetworks
    singular: stacksnetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ready actors
      jsonPath: .status.readyActors
      name: Ready
      type: integer
    - description: Desired actors
      jsonPath: .status.desiredActors
      name: Desired
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StacksNetwork declares the desired topology of one Stacks regtest
          network.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StacksNetworkSpec defines an aggregate regtest topology.
            properties:
              bitcoinBlockProduction:
                description: BitcoinBlockProduction optionally maintains fixed-cadence
                  regtest blocks.
                properties:
                  address:
                    description: Address receives generated regtest coinbase outputs.
                    maxLength: 128
                    minLength: 14
                    type: string
                  intervalSeconds:
                    description: IntervalSeconds is the delay after a completed dispatch;
                      missed ticks are discarded.
                    format: int32
                    maximum: 86400
                    minimum: 1
                    type: integer
                  paused:
                    description: Paused stops new dispatches without cancelling one
                      already armed.
                    type: boolean
                  target:
                    description: Target names a Bitcoin actor in the owning network.
                    maxLength: 63
                    pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                    type: string
                required:
                - address
                - intervalSeconds
                - target
                type: object
              bitcoinNodes:
                items:
                  description: BitcoinNodeTemplate is the aggregate declaration for
                    one Bitcoin node.
                  properties:
                    config:
                      description: ConfigSource selects generated, inline, ConfigMap,
                        or Secret configuration.
                      properties:
                        configMapRef:
                          description: ConfigObjectRef identifies one complete configuration
                            file.
                          properties:
                            expectedDigest:
                              description: ExpectedDigest triggers a restart and verifies
                                mounted bytes before actor startup.
                              pattern: ^sha256:[0-9a-f]{64}$
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        generated:
                          description: GeneratedConfig selects one bounded built-in
                            configuration profile.
                          properties:
                            bootstrapPeers:
                              items:
                                type: string
                              maxItems: 64
                              type: array
                              x-kubernetes-list-type: set
                            eventDispatcher:
                              enum:
                              - queued
                              - blocking
                              type: string
                            profile:
                              enum:
                              - bitcoin-regtest/v1
                              - nakamoto-regtest-node/v1
                              type: string
                            seed:
                              description: Seed pins a generated Stacks node identity.
                                Empty derives from network and actor names.
                              type: string
                          required:
                          - profile
                          type: object
                        inline:
                          description: InlineConfig contains public, non-secret actor
                            configuration.
                          properties:
                            data:
                              minLength: 1
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                          required:
                          - data
                          type: object
                        secretRef:
                          description: ConfigObjectRef identifies one complete configuration
                            file.
                          properties:
                            expectedDigest:
                              description: ExpectedDigest triggers a restart and verifies
                                mounted bytes before actor startup.
                              pattern: ^sha256:[0-9a-f]{64}$
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one configuration source is required
                        rule: '(has(self.generated) ? 1 : 0) + (has(self.inline) ?
                          1 : 0) + (has(self.configMapRef) ? 1 : 0) + (has(self.secretRef)
                          ? 1 : 0) == 1'
                    container:
                      description: |-
                        ContainerOverride exposes explicit process controls for version-specific images.
                        Operator-owned environment names are reserved and cannot be overridden.
                      properties:
                        args:
                          items:
                            type: string
                          maxItems: 128
                          type: array
                        command:
                          items:
                            type: string
                          maxItems: 32
                          minItems: 1
                          type: array
                        env:
                          additionalProperties:
                            type: string
                          maxProperties: 128
                          type: object
                        workingDir:
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: container environment must not override operator-owned
                          variables
                        rule: '!has(self.env) || !self.env.exists(key, key in [''POD_IP'',
                          ''STACKS_ACTOR'', ''STACKS_ACTOR_ROLE'', ''STACKS_CONFIG_RENDERED'',
                          ''STACKS_CONFIG_TEMPLATE'', ''STACKS_NETWORK'', ''STACKS_SERVICE_MAP''])'
                    image:
                      type: string
                    name:
                      maxLength: 63
                      pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                      type: string
                    peerRefs:
                      items:
                        type: string
                      maxItems: 31
                      type: array
                      x-kubernetes-list-type: set
                    role:
                      description: BitcoinNodeRole describes a Bitcoin Core node's
                        topology role.
                      enum:
                      - miner
                      - follower
                      type: string
                    suspended:
                      type: boolean
                    workload:
                      description: WorkloadSpec contains shared resource, storage,
                        and scheduling controls.
                      properties:
                        nodeSelector:
                          additionalProperties:
                            type: string
                          type: object
                        resources:
                          description: ResourceSpec contains CPU and memory requests
                            and limits for one actor.
                          properties:
                            limits:
                              description: ResourceValues contains Kubernetes quantity
                                strings for CPU and memory.
                              properties:
                                cpu:
                                  type: string
                                memory:
                                  type: string
                              type: object
                            requests:
                              description: ResourceValues contains Kubernetes quantity
                                strings for CPU and memory.
                              properties:
                                cpu:
                                  type: string
                                memory:
                                  type: string
                              type: object
                          type: object
                        storage:
                          description: StorageSpec configures an actor data volume.
                          properties:
                            accessModes:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                            enabled:
                              type: boolean
                            mountPath:
                              type: string
                            retainOnDelete:
                              type: boolean
                            size:
                              pattern: ^[0-9]+(?:Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                              type: string
                            storageClassName:
                              type: string
                          type: object
                        terminationGracePeriodSeconds:
                          format: int64
                          minimum: 0
                          type: integer
                        tolerations:
                          items:
                            description: TolerationSpec is the bounded subset of Pod
                              toleration fields used by actors.
                            properties:
                              effect:
                                enum:
                                - NoSchedule
                                - PreferNoSchedule
                                - NoExecute
                                type: string
                              key:
                                type: string
                              operator:
                                description: A toleration operator is the set of operators
                                  that can be used in a toleration.
                                enum:
                                - Exists
                                - Equal
                                type: string
                              tolerationSeconds:
                                format: int64
                                type: integer
                              value:
                                type: string
                            type: object
                          type: array
                      type: object
                  required:
                  - config
                  - name
                  - role
                  type: object
                maxItems: 32
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              defaults:
                description: NetworkDefaults supplies values inherited by compiled
                  leaf resources.
                properties:
                  bitcoinImage:
                    type: string
                  dependencyImage:
                    type: string
                  imagePullPolicy:
                    description: PullPolicy describes a policy for if/when to pull
                      a container image
                    type: string
                  imagePullSecrets:
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  stacksNodeImage:
                    type: string
                  stacksSignerImage:
                    type: string
                  workload:
                    description: WorkloadSpec contains shared resource, storage, and
                      scheduling controls.
                    properties:
                      nodeSelector:
                        additionalProperties:
                          type: string
                        type: object
                      resources:
                        description: ResourceSpec contains CPU and memory requests
                          and limits for one actor.
                        properties:
                          limits:
                            description: ResourceValues contains Kubernetes quantity
                              strings for CPU and memory.
                            properties:
                              cpu:
                                type: string
                              memory:
                                type: string
                            type: object
                          requests:
                            description: ResourceValues contains Kubernetes quantity
                              strings for CPU and memory.
                            properties:
                              cpu:
                                type: string
                              memory:
                                type: string
                            type: object
                        type: object
                      storage:
                        description: StorageSpec configures an actor data volume.
                        properties:
                          accessModes:
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          enabled:
                            type: boolean
                          mountPath:
                            type: string
                          retainOnDelete:
                            type: boolean
                          size:
                            pattern: ^[0-9]+(?:Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                            type: string
                          storageClassName:
                            type: string
                        type: object
                      terminationGracePeriodSeconds:
                        format: int64
                        minimum: 0
                        type: integer
                      tolerations:
                        items:
                          description: TolerationSpec is the bounded subset of Pod
                            toleration fields used by actors.
                          properties:
                            effect:
                              enum:
                              - NoSchedule
                              - PreferNoSchedule
                              - NoExecute
                              type: string
                            key:
                              type: string
                            operator:
                              description: A toleration operator is the set of operators
                                that can be used in a toleration.
                              enum:
                              - Exists
                              - Equal
                              type: string
                            tolerationSeconds:
                              format: int64
                              type: integer
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              genesis:
                description: GenesisSpec contains network-wide values included by
                  generated node profiles.
                properties:
                  balances:
                    items:
                      description: GenesisBalance grants one address an initial micro-STX
                        balance.
                      properties:
                        address:
                          type: string
                        amount:
                          format: int64
                          minimum: 1
                          type: integer
                      required:
                      - address
                      - amount
                      type: object
                    maxItems: 1000
                    type: array
                    x-kubernetes-list-map-keys:
                    - address
                    x-kubernetes-list-type: map
                type: object
              signers:
                items:
                  description: StacksSignerTemplate is the aggregate declaration for
                    one Stacks signer.
                  properties:
                    config:
                      description: ConfigSource selects generated, inline, ConfigMap,
                        or Secret configuration.
                      properties:
                        configMapRef:
                          description: ConfigObjectRef identifies one complete configuration
                            file.
                          properties:
                            expectedDigest:
                              description: ExpectedDigest triggers a restart and verifies
                                mounted bytes before actor startup.
                              pattern: ^sha256:[0-9a-f]{64}$
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        generated:
                          description: GeneratedConfig selects one bounded built-in
                            configuration profile.
                          properties:
                            bootstrapPeers:
                              items:
                                type: string
                              maxItems: 64
                              type: array
                              x-kubernetes-list-type: set
                            eventDispatcher:
                              enum:
                              - queued
                              - blocking
                              type: string
                            profile:
                              enum:
                              - bitcoin-regtest/v1
                              - nakamoto-regtest-node/v1
                              type: string
                            seed:
                              description: Seed pins a generated Stacks node identity.
                                Empty derives from network and actor names.
                              type: string
                          required:
                          - profile
                          type: object
                        inline:
                          description: InlineConfig contains public, non-secret actor
                            configuration.
                          properties:
                            data:
                              minLength: 1
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                          required:
                          - data
                          type: object
                        secretRef:
                          description: ConfigObjectRef identifies one complete configuration
                            file.
                          properties:
                            expectedDigest:
                              description: ExpectedDigest triggers a restart and verifies
                                mounted bytes before actor startup.
                              pattern: ^sha256:[0-9a-f]{64}$
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one configuration source is required
                        rule: '(has(self.generated) ? 1 : 0) + (has(self.inline) ?
                          1 : 0) + (has(self.configMapRef) ? 1 : 0) + (has(self.secretRef)
                          ? 1 : 0) == 1'
                    container:
                      description: |-
                        ContainerOverride exposes explicit process controls for version-specific images.
                        Operator-owned environment names are reserved and cannot be overridden.
                      properties:
                        args:
                          items:
                            type: string
                          maxItems: 128
                          type: array
                        command:
                          items:
                            type: string
                          maxItems: 32
                          minItems: 1
                          type: array
                        env:
                          additionalProperties:
                            type: string
                          maxProperties: 128
                          type: object
                        workingDir:
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: container environment must not override operator-owned
                          variables
                        rule: '!has(self.env) || !self.env.exists(key, key in [''POD_IP'',
                          ''STACKS_ACTOR'', ''STACKS_ACTOR_ROLE'', ''STACKS_CONFIG_RENDERED'',
                          ''STACKS_CONFIG_TEMPLATE'', ''STACKS_NETWORK'', ''STACKS_SERVICE_MAP''])'
                    image:
                      type: string
                    index:
                      format: int32
                      minimum: 0
                      type: integer
                    name:
                      maxLength: 63
                      pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                      type: string
                    nodeRef:
                      maxLength: 63
                      pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                      type: string
                    publicKey:
                      type: string
                    serviceRefs:
                      description: ServiceRefs expose additional declared actor Services
                        to raw configuration templates.
                      items:
                        type: string
                      maxItems: 8
                      type: array
                      x-kubernetes-list-type: set
                    suspended:
                      type: boolean
                    weight:
                      format: int64
                      maximum: 9007199254740991
                      minimum: 1
                      type: integer
                    workload:
                      description: WorkloadSpec contains shared resource, storage,
                        and scheduling controls.
                      properties:
                        nodeSelector:
                          additionalProperties:
                            type: string
                          type: object
                        resources:
                          description: ResourceSpec contains CPU and memory requests
                            and limits for one actor.
                          properties:
                            limits:
                              description: ResourceValues contains Kubernetes quantity
                                strings for CPU and memory.
                              properties:
                                cpu:
                                  type: string
                                memory:
                                  type: string
                              type: object
                            requests:
                              description: ResourceValues contains Kubernetes quantity
                                strings for CPU and memory.
                              properties:
                                cpu:
                                  type: string
                                memory:
                                  type: string
                              type: object
                          type: object
                        storage:
                          description: StorageSpec configures an actor data volume.
                          properties:
                            accessModes:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                            enabled:
                              type: boolean
                            mountPath:
                              type: string
                            retainOnDelete:
                              type: boolean
                            size:
                              pattern: ^[0-9]+(?:Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                              type: string
                            storageClassName:
                              type: string
                          type: object
                        terminationGracePeriodSeconds:
                          format: int64
                          minimum: 0
                          type: integer
                        tolerations:
                          items:
                            description: TolerationSpec is the bounded subset of Pod
                              toleration fields used by actors.
                            properties:
                              effect:
                                enum:
                                - NoSchedule
                                - PreferNoSchedule
                                - NoExecute
                                type: string
                              key:
                                type: string
                              operator:
                                description: A toleration operator is the set of operators
                                  that can be used in a toleration.
                                enum:
                                - Exists
                                - Equal
                                type: string
                              tolerationSeconds:
                                format: int64
                                type: integer
                              value:
                                type: string
                            type: object
                          type: array
                      type: object
                  required:
                  - config
                  - index
                  - name
                  - nodeRef
                  - weight
                  type: object
                  x-kubernetes-validations:
                  - message: signer configuration must use a Secret reference
                    rule: has(self.config.secretRef)
                maxItems: 100
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              stacksNodes:
                items:
                  description: StacksNodeTemplate is the aggregate declaration for
                    one Stacks node.
                  properties:
                    bitcoinNodeRef:
                      maxLength: 63
                      pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                      type: string
                    config:
                      description: ConfigSource selects generated, inline, ConfigMap,
                        or Secret configuration.
                      properties:
                        configMapRef:
                          description: ConfigObjectRef identifies one complete configuration
                            file.
                          properties:
                            expectedDigest:
                              description: ExpectedDigest triggers a restart and verifies
                                mounted bytes before actor startup.
                              pattern: ^sha256:[0-9a-f]{64}$
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        generated:
                          description: GeneratedConfig selects one bounded built-in
                            configuration profile.
                          properties:
                            bootstrapPeers:
                              items:
                                type: string
                              maxItems: 64
                              type: array
                              x-kubernetes-list-type: set
                            eventDispatcher:
                              enum:
                              - queued
                              - blocking
                              type: string
                            profile:
                              enum:
                              - bitcoin-regtest/v1
                              - nakamoto-regtest-node/v1
                              type: string
                            seed:
                              description: Seed pins a generated Stacks node identity.
                                Empty derives from network and actor names.
                              type: string
                          required:
                          - profile
                          type: object
                        inline:
                          description: InlineConfig contains public, non-secret actor
                            configuration.
                          properties:
                            data:
                              minLength: 1
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                          required:
                          - data
                          type: object
                        secretRef:
                          description: ConfigObjectRef identifies one complete configuration
                            file.
                          properties:
                            expectedDigest:
                              description: ExpectedDigest triggers a restart and verifies
                                mounted bytes before actor startup.
                              pattern: ^sha256:[0-9a-f]{64}$
                              type: string
                            key:
                              type: string
                            mountPath:
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one configuration source is required
                        rule: '(has(self.generated) ? 1 : 0) + (has(self.inline) ?
                          1 : 0) + (has(self.configMapRef) ? 1 : 0) + (has(self.secretRef)
                          ? 1 : 0) == 1'
                    container:
                      description: |-
                        ContainerOverride exposes explicit process controls for version-specific images.
                        Operator-owned environment names are reserved and cannot be overridden.
                      properties:
                        args:
                          items:
                            type: string
                          maxItems: 128
                          type: array
                        command:
                          items:
                            type: string
                          maxItems: 32
                          minItems: 1
                          type: array
                        env:
                          additionalProperties:
                            type: string
                          maxProperties: 128
                          type: object
                        workingDir:
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: container environment must not override operator-owned
                          variables
                        rule: '!has(self.env) || !self.env.exists(key, key in [''POD_IP'',
                          ''STACKS_ACTOR'', ''STACKS_ACTOR_ROLE'', ''STACKS_CONFIG_RENDERED'',
                          ''STACKS_CONFIG_TEMPLATE'', ''STACKS_NETWORK'', ''STACKS_SERVICE_MAP''])'
                    image:
                      type: string
                    name:
                      maxLength: 63
                      pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                      type: string
                    role:
                      description: StacksNodeRole describes a Stacks node process
                        role.
                      enum:
                      - miner
                      - follower
                      - signer-node
                      type: string
                    serviceRefs:
                      description: ServiceRefs expose additional declared actor Services
                        to raw configuration templates.
                      items:
                        type: string
                      maxItems: 8
                      type: array
                      x-kubernetes-list-type: set
                    suspended:
                      type: boolean
                    workload:
                      description: WorkloadSpec contains shared resource, storage,
                        and scheduling controls.
                      properties:
                        nodeSelector:
                          additionalProperties:
                            type: string
                          type: object
                        resources:
                          description: ResourceSpec contains CPU and memory requests
                            and limits for one actor.
                          properties:
                            limits:
                              description: ResourceValues contains Kubernetes quantity
                                strings for CPU and memory.
                              properties:
                                cpu:
                                  type: string
                                memory:
                                  type: string
                              type: object
                            requests:
                              description: ResourceValues contains Kubernetes quantity
                                strings for CPU and memory.
                              properties:
                                cpu:
                                  type: string
                                memory:
                                  type: string
                              type: object
                          type: object
                        storage:
                          description: StorageSpec configures an actor data volume.
                          properties:
                            accessModes:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                            enabled:
                              type: boolean
                            mountPath:
                              type: string
                            retainOnDelete:
                              type: boolean
                            size:
                              pattern: ^[0-9]+(?:Ei|Pi|Ti|Gi|Mi|Ki|E|P|T|G|M|K)?$
                              type: string
                            storageClassName:
                              type: string
                          type: object
                        terminationGracePeriodSeconds:
                          format: int64
                          minimum: 0
                          type: integer
                        tolerations:
                          items:
                            description: TolerationSpec is the bounded subset of Pod
                              toleration fields used by actors.
                            properties:
                              effect:
                                enum:
                                - NoSchedule
                                - PreferNoSchedule
                                - NoExecute
                                type: string
                              key:
                                type: string
                              operator:
                                description: A toleration operator is the set of operators
                                  that can be used in a toleration.
                                enum:
                                - Exists
                                - Equal
                                type: string
                              tolerationSeconds:
                                format: int64
                                type: integer
                              value:
                                type: string
                            type: object
                          type: array
                      type: object
                  required:
                  - bitcoinNodeRef
                  - config
                  - name
                  - role
                  type: object
                  x-kubernetes-validations:
                  - message: miner configuration must use a Secret reference
                    rule: self.role != 'miner' || has(self.config.secretRef)
                maxItems: 100
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              suspended:
                type: boolean
            required:
            - bitcoinNodes
            - defaults
            type: object
            x-kubernetes-validations:
            - message: changing the initial production target requires a fresh environment
              rule: '!has(oldSelf.bitcoinBlockProduction) || !has(self.bitcoinBlockProduction)
                || self.bitcoinBlockProduction.target == oldSelf.bitcoinBlockProduction.target'
            - message: production must reference a declared Bitcoin node
              rule: '!has(self.bitcoinBlockProduction) || self.bitcoinNodes.exists(node,
                node.name == self.bitcoinBlockProduction.target)'
            - message: every Stacks node must reference a declared Bitcoin node
              rule: '!has(self.stacksNodes) || self.stacksNodes.all(node, self.bitcoinNodes.exists(bitcoin,
                bitcoin.name == node.bitcoinNodeRef))'
            - message: every signer must reference a declared signer-node
              rule: '!has(self.signers) || (has(self.stacksNodes) && self.signers.all(signer,
                self.stacksNodes.exists(node, node.name == signer.nodeRef && node.role
                == ''signer-node'')))'
            - message: a signer-node may be referenced by only one signer
              rule: '!has(self.signers) || self.signers.all(signer, self.signers.exists_one(other,
                other.nodeRef == signer.nodeRef))'
            - message: signer indices must be unique
              rule: '!has(self.signers) || self.signers.all(signer, self.signers.exists_one(other,
                other.index == signer.index))'
            - message: Stacks node serviceRefs must name declared actors
              rule: '!has(self.stacksNodes) || self.stacksNodes.all(node, !has(node.serviceRefs)
                || node.serviceRefs.all(ref, self.bitcoinNodes.exists(actor, actor.name
                == ref) || (has(self.stacksNodes) && self.stacksNodes.exists(actor,
                actor.name == ref)) || (has(self.signers) && self.signers.exists(actor,
                actor.name == ref))))'
            - message: signer serviceRefs must name declared actors
              rule: '!has(self.signers) || self.signers.all(signer, !has(signer.serviceRefs)
                || signer.serviceRefs.all(ref, self.bitcoinNodes.exists(actor, actor.name
                == ref) || (has(self.stacksNodes) && self.stacksNodes.exists(actor,
                actor.name == ref)) || (has(self.signers) && self.signers.exists(actor,
                actor.name == ref))))'
          status:
            description: StacksNetworkStatus aggregates leaf readiness and admitted
              identity.
            properties:
              actors:
                items:
                  description: ActorIdentity is the immutable admitted identity of
                    one running actor.
                  properties:
                    configDigest:
                      type: string
                    controllerRevision:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    podName:
                      type: string
                    podUID:
                      type: string
                    requestedImage:
                      type: string
                    resourceName:
                      type: string
                    role:
                      type: string
                    runtimeImageID:
                      type: string
                    serviceName:
                      type: string
                    specDigest:
                      type: string
                    statefulSetName:
                      type: string
                    statefulSetUID:
                      type: string
                  required:
                  - configDigest
                  - controllerRevision
                  - kind
                  - name
                  - podName
                  - podUID
                  - requestedImage
                  - resourceName
                  - role
                  - runtimeImageID
                  - serviceName
                  - specDigest
                  - statefulSetName
                  - statefulSetUID
                  type: object
                maxItems: 232
                type: array
              bitcoinProductionUID:
                description: BitcoinProductionUID permanently pins the production
                  ledger for this network incarnation.
                type: string
              children:
                items:
                  description: ChildStatus summarizes one compiled topology resource.
                  properties:
                    actorName:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    ready:
                      type: boolean
                  required:
                  - actorName
                  - kind
                  - name
                  - ready
                  type: object
                maxItems: 232
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              desiredActors:
                format: int32
                type: integer
              inventoryDigest:
                type: string
              inventoryObservedAt:
                format: date-time
                type: string
              inventoryReady:
                type: boolean
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              readyActors:
                format: int32
                type: integer
              targetDeclarations:
                description: TargetDeclarations reports compiled intent without requiring
                  global readiness.
                properties:
                  actors:
                    description: Actors lists compiled leaf declarations in stable
                      kind/name order.
                    items:
                      description: TargetDeclaration binds a named actor to its complete
                        compiled leaf specification.
                      properties:
                        actorName:
                          description: ActorName is the logical actor name in the
                            parent topology.
                          maxLength: 63
                          pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                          type: string
                        kind:
                          description: Kind is the leaf resource kind.
                          enum:
                          - BitcoinNode
                          - StacksNode
                          - StacksSigner
                          type: string
                        name:
                          description: Name is the compiled same-namespace resource
                            name.
                          maxLength: 63
                          pattern: ^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$
                          type: string
                        specDigest:
                          description: SpecDigest uses the existing canonical leaf-specification
                            digest contract.
                          pattern: ^sha256:[0-9a-f]{64}$
                          type: string
                        suspended:
                          description: Suspended is the effective parent-or-actor
                            suspension state.
                          type: boolean
                      required:
                      - actorName
                      - kind
                      - name
                      - specDigest
                      - suspended
                      type: object
                    maxItems: 232
                    type: array
                    x-kubernetes-list-map-keys:
                    - kind
                    - name
                    x-kubernetes-list-type: map
                  networkUID:
                    description: NetworkUID prevents admission across parent recreation.
                    minLength: 1
                    type: string
                  observedGeneration:
                    description: ObservedGeneration binds compilation to a particular
                      admitted parent spec.
                    format: int64
                    minimum: 1
                    type: integer
                  schemaVersion:
                    description: SchemaVersion identifies the entry and digest interpretation.
                    enum:
                    - network.stacks.org/target-declarations/v1
                    type: string
                required:
                - actors
                - networkUID
                - observedGeneration
                - schemaVersion
                type: object
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: metadata.name must be a DNS label no longer than 63 characters
          rule: self.metadata.name.size() <= 63 && self.metadata.name.matches('^[a-z0-9]([-a-z0-9]*[a-z0-9])?$')
    served: true
    storage: true
    subresources:
      status: {}
