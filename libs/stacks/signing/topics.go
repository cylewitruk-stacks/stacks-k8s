package signing

const (
	// TopicStackSTX identifies the stack-stx signing topic.
	TopicStackSTX = "stack-stx"
	// TopicStackExtend identifies the stack-extend signing topic.
	TopicStackExtend = "stack-extend"
	// TopicStackIncrease identifies the stack-increase signing topic.
	TopicStackIncrease = "stack-increase"
	// TopicAggregateCommit identifies the agg-commit signing topic.
	TopicAggregateCommit = "agg-commit"
	// TopicAggregateIncrease identifies the agg-increase signing topic.
	TopicAggregateIncrease = "agg-increase"
	// TopicGrantAuthorization identifies the grant-authorization signing topic.
	TopicGrantAuthorization = "grant-authorization"
)
