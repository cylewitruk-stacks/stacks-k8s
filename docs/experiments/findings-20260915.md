# Experiment findings ledger — 2026-09-15

Scope and evidence: [Bitcoin split-view and persistent Stacks burnchain stall](bitcoin-split-anchor-stall.md).

## stacks-k8s findings

| ID | Priority / disposition | Finding | Resolution or follow-up |
| --- | --- | --- | --- |
| W9 | High / implemented and live-qualified | The recorder observed NetworkChaos but omitted PodChaos and StressChaos, leaving actor failure and resource-stress phases without native lifecycle rows. | One domain-owned allowlist now drives watches and RBAC for all three namespaced fault kinds. API-server tests cover sources and permissions; live no-target probes produced exact source rows. |
| W10 | High / diagnostic correction | The experiment showed a persistent 35-block separation between observed Bitcoin and Stacks processed heights. | Expose exact-identity height samples independently of Operational. Instantaneous height differences cannot establish elapsed lag or chain agreement; a reliable stall gate remains deferred. The recorded live run exercised the superseded gate, not this correction. |
| W11 | Low / documentation | Replacing a mutable telemetry credential Secret under the same name does not roll collectors and violates the intended destination-identity contract. | Treat backend credentials as immutable. Change destination identity by creating a new immutable Secret and a new recording. No controller Secret watch or automatic credential roll is added. |
| W12 | Verified behavior | Kubernetes readiness preceded native RPC/protocol recovery after deliberate Stacks-node Pod churn. | Existing `WorkloadReady`, native observation and aggregate `Operational` states remained distinct. No runtime change is needed. |
| W13 | Bounded behavior | The larger contract profile was rejected while a smaller wide-map/nested-call profile completed under CPU and network stress. | Preserve finite external batches and exact transaction receipts. This run does not justify an in-cluster scenario engine or automatic overload generator. |

## Upstream hypothesis

| ID | Confidence | Observation | Recommended reduction |
| --- | --- | --- | --- |
| U1 | Reproduced once; not reduced | After a cycle-boundary Bitcoin split and bounded reorganization, three Stacks 4.0.3 nodes agreed on one Stacks tip but remained at burn height 359 while three Bitcoin nodes converged and advanced to 394. Missing PoX anchor and prepare-ancestor errors continued on every node. | Re-run a matrix that independently removes the third-node failure, asymmetric branch length, cycle-boundary crossing and explicit reorganization. Preserve exact Bitcoin branches, sortition identifiers and node logs. Report upstream only after a minimal repeatable trigger is confirmed. |

No new vulnerability is claimed. `U1` is an evidence-backed defensive investigation
lead. The implemented stacks-k8s changes improve detection and correlation without
classifying the root cause or changing actor behavior.
