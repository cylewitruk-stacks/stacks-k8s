package foundation

const (
	// ModeController identifies the controller worker entrypoint.
	ModeController = "controller"
	// ModeResolveKey identifies the resolve-key worker entrypoint.
	ModeResolveKey = "resolve-key"
)
